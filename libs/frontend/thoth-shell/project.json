{
  "name": "@ptah-extension/thoth-shell",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/frontend/thoth-shell/src",
  "prefix": "ptah",
  "projectType": "library",
  "tags": ["scope:webview", "type:feature", "platform:angular"],
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/frontend/thoth-shell/jest.config.ts",
        "tsConfig": "libs/frontend/thoth-shell/tsconfig.spec.json"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx ngc --noEmit --project libs/frontend/thoth-shell/tsconfig.lib.json"
      }
    }
  }
}
