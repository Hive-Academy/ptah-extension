/**
 * Native Popover - Barrel Export
 *
 * Modal-like popover with native focus management.
 * Uses Floating UI instead of CDK Overlay + FocusTrap.
 *
 * @module native/popover
 */
export { NativePopoverComponent } from './native-popover.component';
