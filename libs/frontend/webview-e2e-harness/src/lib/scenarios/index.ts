/**
 * Scenario builders barrel.
 *
 * Intentionally empty in P3.B1. P3.B2 (chat-flow specs) and P3.B3
 * (session/monitor/settings specs) will land scenario factories here —
 * e.g. `buildChatSession`, `seedSessionsList` — that compose
 * `bridge.inject(...)` calls into reusable helpers.
 */
export {};
