{
  "name": "@ptah-extension/chat-streaming",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/frontend/chat-streaming/src",
  "prefix": "ptah",
  "projectType": "library",
  "tags": ["scope:webview", "type:feature"],
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/frontend/chat-streaming/jest.config.ts",
        "tsConfig": "libs/frontend/chat-streaming/tsconfig.spec.json"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx ngc --noEmit --project libs/frontend/chat-streaming/tsconfig.lib.json"
      }
    }
  }
}
