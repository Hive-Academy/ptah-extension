# llm-abstraction

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build llm-abstraction` to build the library.

## Running unit tests

Run `nx test llm-abstraction` to execute the unit tests via [Jest](https://jestjs.io).
