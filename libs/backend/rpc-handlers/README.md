# rpc-handlers

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build rpc-handlers` to build the library.

## Running unit tests

Run `nx test rpc-handlers` to execute the unit tests via [Jest](https://jestjs.io).
