# template-generation

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build template-generation` to build the library.

## Running unit tests

Run `nx test template-generation` to execute the unit tests via [Jest](https://jestjs.io).
