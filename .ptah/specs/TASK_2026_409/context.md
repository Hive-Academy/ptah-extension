User requested the documented production backup and local installer build. Existing version and source are 0.1.70. Observed Windows PowerShell 5 JSON array failure in offline guard, plus electron-builder 26.8.1 rejects win.sign. Minimal bugfix workflow: devops engineer repairs with regression tests, root reviews and runs backup/package. Preserve safeguards and production identity. No installation until backup succeeds.
