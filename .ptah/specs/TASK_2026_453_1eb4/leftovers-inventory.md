# Leftovers inventory — TASK_2026_437 + TASK_2026_453 follow-ups

Read-only inventory, 2026-09-16. No tests, builds, git writes or `git fetch` were run.
"origin/main" means the local ref `97239e814` (PR #523, 2026-09-16 17:08). A stale ref is possible.
Abbreviations: `S437` = `.ptah/specs/TASK_2026_437_0778/`, `S453` = `.ptah/specs/TASK_2026_453_1eb4/`.

Note on sources: `.ptah/specs/property-hub-reference-evaluation.md` is a course-content evaluation of
property-hub (licensing, seed data, docs). It does not define the load test. The load-test
definition is `S437/handoff.md:480-490` (§9).

## Open PRs (`gh pr list --state open`)

| PR   | Head → base                                                     | Overlap                                                                                                                                         |
| ---- | --------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| #524 | `perf/task-453-tile-open-long-tasks` → `main` (draft, 65 files) | This branch. It holds all of Group B.                                                                                                           |
| #457 | `main` → `release/cli` ("release cli", open since 2026-09-04)   | None with A or B. Note: CLAUDE.md bans PRs against release branches; release uses the Sync Release Branch workflow. Consider closing it (user). |

No open PR touches `publish-electron.yml`, the CI skip guard, `internalQuery`, or load-test scripts.
Drift check: `git diff 51d0d2e1f origin/main` has no change to the chat, core app-state,
`session-rpc.handlers.ts`, `session-history-reader.service.ts` or e2e perf support files. Main drift
is in canvas layout files (PR #514, #520) only; Batch 3 edited `orchestra-canvas.component.ts`,
which main did not change.

## Table A — TASK_2026_437 leftovers

| #   | Item                                                              | Definition (source)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | State on origin/main                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Files likely touched                                                                                                                                                                                                                                           | Human / outward-facing                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Size                                                                         | Verification                                                                                                                                                                                                                                                | Codex lane                                                                                                                                     |
| --- | ----------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| A1  | D10 `publish-electron.yml` build matrix                           | "Release CI build matrix green before P2 is declared done … builds and publishes only win32-x64 (windows-latest), darwin-arm64 (macos-latest) and linux-x64 … **OPEN**: not yet run on this branch; it runs on a `release/electron` push or `workflow_dispatch`" (`S437/batches.md:674`, `:701`; `S437/handoff.md:273-277`, `:363-364`, `:371`). Each leg runs `verify-packed-native.js` with the `@parcel/watcher` require + subscribe gate.                                                                                                                                                                                                                         | **NOT done.** Last `publish-electron` run is `33881075939`, 2026-09-04, on `release/electron` `1dd048dd8` — before PR #510 (2026-09-15) and PR #512 better-sqlite3 13 (2026-09-14). No dispatch or release push since. The same run would also prove the #512 packaged native check on all OSes (`S437/handoff.md:453-455`).                                                                                                                                                                                                                                                       | None for the proof itself. Optional: `.github/workflows/publish-electron.yml` if a dry-run input is added.                                                                                                                                                     | **YES, outward-facing and irreversible.** `workflow_dispatch` (only input: `bump` patch/minor/major, `publish-electron.yml:46-57`) runs `prepare` (version bump, tag check with `RELEASE_TOKEN`), signs `Ptah.exe` + installer with SSL.com eSigner (billed ~$1 per signature, ~$2 per release, `:8`, `:457-627`), then `release` creates a git tag, a `chore/bump-electron-v*` PR (`:651-657`) and a public GitHub Release `Ptah Desktop vX` (`:706-710`). There is no dry-run mode. | S (dispatch) / M (add a `dry_run` input that skips tag, signing and release) | All 3 matrix jobs green, including "Verify packed native deps (better-sqlite3 ABI + @parcel/watcher …)" on each OS. Record the run id in `S437/batches.md` Batch 10 outcome.                                                                                | Dispatch: no. `dry_run` input: yes (YAML only), but the dispatch that proves it is still a user action and costs macOS/Windows runner minutes. |
| A2  | FU-16b-c `internalQuery` slot starvation (+ FU-16c default drift) | FU-16b-c: "the two background lanes can hold both global slots (global 2, per lane 1), so a wizard or `user-action` call can hit its 60 s queue timeout behind a 90–120 s synthesis/archaeologist call or an untimed curator call. Options: raise the global limit to 3 and cap background at limit − 1; cap background without raising (regresses TASK_2026_352); or let `user-action` overshoot the global limit by one. Deferred." (`S437/batches.md:1155`; `S437/handoff.md:372-373`, `:409`). FU-16c: "platform-core `internalQuery.maxConcurrent` default 1 vs agent-sdk `DEFAULT_MAX_CONCURRENT` 2" (`S437/batches.md:1128`, `S437/handoff.md:408`).           | **NOT done.** `internal-query-concurrency-gate.ts:87` `DEFAULT_MAX_CONCURRENT = 2`, `:103` per lane 1, `:33` `USER_ACTION_QUERY_LANE`. Drift still present: `libs/backend/platform-core/src/file-settings-keys.ts:631` `'internalQuery.maxConcurrent': 1`. Stale doc: `libs/backend/agent-sdk/src/lib/errors/internal-query-queue-timeout.error.ts:6` says "default limit 1".                                                                                                                                                                                                      | `libs/backend/agent-sdk/src/lib/internal-query/internal-query-concurrency-gate.ts` + spec, `internal-query.service.ts` + spec, `libs/backend/platform-core/src/file-settings-keys.ts` (+ its spec), the error file doc, `libs/backend/agent-sdk/CLAUDE.md:87`. | **User decision** on the option (1/2/3). No outward action.                                                                                                                                                                                                                                                                                                                                                                                                                           | FU-16c drift fix S; FU-16b-c M                                               | `npx nx run-many -t test -p @ptah-extension/agent-sdk @ptah-extension/platform-core --parallel=1 -- --maxWorkers=2` (header 2); gate spec for "user-action admitted while both background lanes hold slots"; typecheck + lint; degradation audit TOTAL 303. | Yes, after the decision. The FU-16c drift fix can go now.                                                                                      |
| A3  | `chore/bump-*` CI skip guard                                      | "CI skip rule for `chore/bump-*` branches (PR #512 showed `main`, `electron-e2e`, `vscode-e2e` SKIPPED)" (`S437/handoff.md:374-375`); "find out why before merging" (`:452-455`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Cause found, not fixed.** The skip is deliberate (commit `287dd7d23`, 2026-06-23, "ci: skip auto-generated version-bump PRs"): `if: … !startsWith(github.head_ref, 'chore/bump-')` in `ci.yml:48`, `electron-e2e.yml:44`, `vscode-e2e.yml:56`. The guard is meant for bot branches `chore/bump-cli-v*` (`publish-cli.yml:401`), `chore/bump-electron-v*` (`publish-electron.yml:657`), `chore/bump-extension-v*` (`publish-extension.yml:226`). A human dependency bump named `chore/bump-better-sqlite3-13` matched it, so PR #512 merged (2026-09-14) with no main CI, no e2e. | The 3 workflow files above. Narrow the guard to the 3 bot prefixes (`chore/bump-cli-v`, `chore/bump-electron-v`, `chore/bump-extension-v`), or rename the bot branches to e.g. `chore/release-bump-*` in the 3 publish workflows and the guards together.      | No outward action. Low risk: if the narrowing is wrong, bot bump PRs run CI again (cost only).                                                                                                                                                                                                                                                                                                                                                                                        | S                                                                            | YAML parse / `actionlint` if available; reason about `head_ref` values; the first bot bump PR after merge must show the jobs SKIPPED and a normal PR must show them running.                                                                                | Yes.                                                                                                                                           |
| A4  | property-hub load-test setup/cleanup scripts                      | "Use a CLONE, never the real repos: `git clone D:/projects/property-hub D:/projects/property-hub-loadtest` … `npm ci`, then add ~16 worktrees under `.claude-worktrees/` … With 3 streaming tiles run: B) agent removes all worktrees in one Bash command; C) `rm -rf node_modules && npm ci`; D) `npx nx run-many -t build`; E) use the git panel during C/D … Setup/cleanup scripts were offered but not written." (`S437/handoff.md:480-490`); open decision "Whether to write the property-hub load-test setup/cleanup scripts" (`:377`). Also confirms FU-11e (`:399`).                                                                                          | **NOT done.** No `loadtest` scripts in the repo; no `D:\projects\property-hub-loadtest` clone exists (only `D:\projects\property-hub`).                                                                                                                                                                                                                                                                                                                                                                                                                                            | New `scripts/perf/property-hub-loadtest-setup.ps1` + `-cleanup.ps1` (or outside the repo, user's choice). Scripts touch only the clone path; they must refuse to run against `D:\projects\property-hub`.                                                       | **User decision** to write them. **Running** the load test is manual: dev app, 3 streaming tiles, agent actions, reading `%APPDATA%\Ptah Dev\logs`. Local-only; not outward-facing. Clone + `npm ci` needs ~238k files of disk and must not run during a perf measurement.                                                                                                                                                                                                            | S (scripts); the manual run is ~1-2 h of user time                           | Script dry-run flag (`-WhatIf`) proof; path guard test; manual run log excerpts (`[event-loop] lag`, `git status timed out`, `[GitProcessGate] saturated`, `ptah-hang.log`).                                                                                | Scripts: yes. Run: no.                                                                                                                         |
| A5  | AC-10 manual boot evidence                                        | AC-10: "Boot on this repo — no `[event-loop] lag` window above 1 000 ms after the window opens" (`S437/implementation-plan.md:802`). Instructions: "boot the dev app with `PTAH_PROFILE_ON_LAG_MS=1000`, open the `D:\projects\ptah-437` folder, wait about 3 min, then read `%APPDATA%\Ptah Dev\logs\Ptah Electron-<date>.log` after the line `[Ptah Electron] Clipboard IPC registered`: every `[event-loop] lag` must have maxMs < 1000; record the governor state lines and the adopter lines (symbol indexer, file-index rebuild, backup, user-layer); `ptah-hang.log` must be empty." (`S437/batches.md:1184`, `:1215`; `S437/handoff.md:185`, `:365`, `:378`). | **NOT done.** No AC-10 log excerpt in `S437` on this worktree or in `D:\projects\ptah-437`. `S437/task.md` on origin/main still says `status: in_progress`.                                                                                                                                                                                                                                                                                                                                                                                                                        | `S437/batches.md` (Batch 17 outcome, evidence excerpt), then `S437/task.md` `status:` line.                                                                                                                                                                    | **YES, manual.** A GUI boot of the dev Electron app on an idle machine (not during the Batch 8 / M2 perf runs). Local only.                                                                                                                                                                                                                                                                                                                                                           | S                                                                            | Log excerpt pasted; hang log empty.                                                                                                                                                                                                                         | No (needs a real window and a person, or a Claude agent with the `run` skill on an idle machine).                                              |

## Table B — TASK_2026_453 follow-ups

Stage 2 file map (from `S453/batches.md`): Batch 12 edits `session-history-replayer.service.ts`,
`session-loader.service.ts` + spec (`:1452-1455`); Batch 13 edits `chat-transcript.component.ts`
/ `.html` and requires `chat-transcript.component.spec.ts`, `.replay-mount.spec.ts`,
`.replay-motion.spec.ts` to stay unedited (`:1512-1536`); Batch 14 edits `perf-session-fixture.ts`
and the perf spec only (`:1558-1559`). Stage 2 forbids `project.json` edits (D13, `:1208`) and any
hunk in `onScroll` (`:1228`). All Stage 2 batches are gated on Batch 8 PASS (`S453/handoff.md:50`).

| #   | Item                                                                                 | Definition + source                                                                                                                                                                                                                                                                                                                                                                          | Severity                                 | Belongs where                                                                                                                                                                                                                                                                                                           | Files                                                                                                           | Size                              | Codex lane                                                           | Blast radius / Stage 2 conflict                                                                                               |
| --- | ------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | --------------------------------- | -------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| B1  | F1 release window vs live growth                                                     | "a live event growing content below during the single rAF / 50 ms release window can still recreate the H1 shape at lower probability. Routed to Batch 8 … and the U1 `onScroll` escalation" (`S453/batches.md:1048-1051`; `b7-code-logic-review.md:74`)                                                                                                                                     | Serious (open)                           | **Already covered**: Batch 8 re-check (in progress) + U1 escalation triggers (`:1189-1190`). No new work unless Batch 8 fails.                                                                                                                                                                                          | —                                                                                                               | —                                 | —                                                                    | —                                                                                                                             |
| B2  | U1 `onScroll` anchoring hardening                                                    | "treat an upward move as anchoring when content height changed since the last scroll event … touches a forbidden method, so it needs the user" (`S453/batches.md:888-894`); conclusion "Not required for Stage 2 (ii). It stays an open escalation path" with 3 triggers (`:1177-1193`)                                                                                                      | Conditional (Serious if a trigger fires) | **User decision, conditional.** Not in Stage 2. Escalate only if Batch 8 / M2 / Task 14.2 trips a trigger; then a new batch inside TASK_2026_453.                                                                                                                                                                       | `chat-transcript.component.ts:571` `onScroll` + scroll specs                                                    | M                                 | Yes, once approved                                                   | Touches the forbidden method every Stage 2 batch checks; must be sequenced after Batch 13 or before Batch 9, never beside 13. |
| B3  | Compaction-targeted reload races in-flight replay                                    | "a compaction-targeted reload can race an in-flight replay because it skips the up-front `applyResumingSession` reset (`session-loader.service.ts:677-691`). No regression test pins the ordering." (`S453/batches.md:775-777`; `b5-code-logic-review-delta.md:196-201`). Current code: guard `if (opts?.reason !== 'compaction' \|\| !targetTabId)` at `session-loader.service.ts:683-690`. | Moderate (pre-existing, carried)         | **Fold into TASK_2026_453, Batch 12** (same loader + `session-loader.service.spec.ts`). Add a Task 12.x AC: a spec that pins ordering for a compaction reload during a live replay claim; fix only if the spec shows a stale-state write, and the loader-growth limit (AC 2, "<= ~5 lines") gets an explicit exception. | `session-loader.service.ts`, `session-loader.service.spec.ts`                                                   | S (test) / M (if a fix is needed) | Yes                                                                  | Same files as Task 12.1 → must be in Batch 12, not a parallel lane.                                                           |
| B4  | Throw mid-replay window                                                              | "a throw mid-replay leaves a short window between the replayer's `finally` clearing the flag and the loader applying `applyResumeFailure`. No throw-path state-transition test." (`S453/batches.md:778-779`). Code: `session-history-replayer.service.ts:224` `finally`; loader `replay(` `:782`, `applyResumeFailure` `:797`, `:807`.                                                       | Minor/Moderate (logic, test gap)         | **Fold into Batch 12** (Task 12.1 already adds `session-history-replayer.older-page.spec.ts` and edits both files). Test-only unless the spec shows a visible `loaded` flash.                                                                                                                                           | replayer spec, `session-loader.service.spec.ts`                                                                 | S                                 | Yes                                                                  | Same files as Task 12.1.                                                                                                      |
| B5  | FU-20a global status `loaded` while a later replay waits                             | "global status may read `loaded` while a later replay waits — noted in chat CLAUDE.md, no fix planned" (`S453/batches.md:597-598`; `implementation-plan.md:385`)                                                                                                                                                                                                                             | Minor (pre-existing, accepted)           | **Drop / backlog** (documented, no fix planned). Re-read after Batch 12 since paging adds `replayOlderPage` admission.                                                                                                                                                                                                  | —                                                                                                               | —                                 | —                                                                    | None.                                                                                                                         |
| B6  | Per-tile DOM sampling + M1 AC 2 PARTIAL                                              | "per-tile DOM sampling in `perf-page-capture.ts` (carried from Batch 2 item 5) is needed before AC 2 can close" (`S453/batches.md:840-842`); "AC 2 PARTIAL (per-tile wall time delivered; DOM <= 2× measured whole-canvas only, the harness has no per-tile DOM field)" (`:822-823`)                                                                                                         | Moderate (an AC stays PARTIAL)           | **Fold into TASK_2026_453, Batch 14 Task 14.1** — add `perf-page-capture.ts` to its files and an AC "per-tile DOM replaying/settled in diagnostics JSON", so M2 (Batch 15) closes AC 2. Not covered today: Task 14.1 records per-tile `historyPage.maxEvents` and replay count only.                                    | `apps/ptah-electron-e2e/src/support/perf-page-capture.ts`, perf spec, possibly `perf-measurement-report.ts`     | S-M                               | Yes                                                                  | e2e-only; same spec file as Task 14.1 → same batch. Budgets 200/1,500 unchanged.                                              |
| B7  | `assertScrollSanity` split                                                           | "Split `assertScrollSanity` out of `perf-measurement-report.ts` before the file gains a ninth export" (`S453/batches.md:335-336`). File has 8 function exports now (`perf-measurement-report.ts:37-168`, `assertScrollSanity` `:153`).                                                                                                                                                       | Minor                                    | **Fold into Batch 14 conditionally**: if Task 14.1's "measurement unusable" check or B6 adds an export there, split first in the same lane. Else backlog.                                                                                                                                                               | `perf-measurement-report.ts`, new `perf-scroll-sanity.ts`, perf spec imports                                    | S                                 | Yes                                                                  | e2e-only.                                                                                                                     |
| B8  | `startTraceCapture` try/catch                                                        | "`startTraceCapture` has no try/catch around CDP calls; reachable only from diagnostic tests" (`S453/batches.md:342`). Code `perf-page-capture.ts:130-156`.                                                                                                                                                                                                                                  | Minor                                    | **Fold into Batch 14** only if B6 brings `perf-page-capture.ts` into that batch; else backlog.                                                                                                                                                                                                                          | `perf-page-capture.ts`                                                                                          | S                                 | Yes                                                                  | Diagnostic path only; the gate test hard-disables trace.                                                                      |
| B9  | Unit coverage for `bucketByTime` / `findRendererMainThread` / `summarizeTraceEvents` | "No unit coverage … the e2e project has no Jest target" (`S453/batches.md:337-338`). Code `perf-diagnostics.ts:71`, `:146`, `:172`.                                                                                                                                                                                                                                                          | Minor                                    | **Separate task.** Needs a Jest target on `ptah-electron-e2e` (a `project.json` edit → `nx reset`), which Stage 2 D13 forbids while lanes share the worktree. Alternative: move the pure helpers to a lib with a Jest target.                                                                                           | `apps/ptah-electron-e2e/project.json`, jest config, new `perf-diagnostics.spec.ts`                              | M                                 | Yes (but `nx reset` must run with no other executor in the worktree) | Would collide with Stage 2 lanes via `nx reset`; do after Batch 15 or on another branch.                                      |
| B10 | message-bubble template string-match assertion                                       | "`message-bubble.component.spec.ts` proves the enabled `bubble-fade-enter` binding by template string match, not a rendered class (jsdom limitation). Replace with a rendered-class assertion when a harness allows it." (`S453/batches.md:704-706`; `b4-code-style-review-delta.md:166`). Code `message-bubble.component.spec.ts:280`.                                                      | Moderate (test quality)                  | **Separate task / backlog.** Blocked on a harness that renders `animate.enter` (jsdom cannot); `webview-e2e-harness` Playwright is the likely home.                                                                                                                                                                     | `libs/frontend/chat/src/lib/components/organisms/message-bubble.component.spec.ts`, possibly a webview-e2e spec | M                                 | Yes                                                                  | None with Stage 2 files.                                                                                                      |
| B11 | Duplicated timer-clear                                                               | "timer-clear logic in `chat-transcript.component.ts` is partly duplicated between the effect and `clearReplayMotionHold()`" (`S453/batches.md:707-708`; `b4-code-style-review-delta.md:145`). Code `:467-469` vs `:693-697`.                                                                                                                                                                 | Minor                                    | **Fold into Batch 13** as a first step: the effect calls `clearReplayMotionHold()`. It saves ~3 lines toward B12. Not inside `onScroll` or retention `cleanup()` code, so Task 13.1 AC 5 still holds.                                                                                                                   | `chat-transcript.component.ts`                                                                                  | S                                 | Yes                                                                  | Same file as Task 13.1 → same lane. `replay-motion.spec.ts` must stay green and unedited.                                     |
| B12 | `chat-transcript.component.ts` at exactly 700 lines                                  | Batch 7 left it "700 lines, at the warn ceiling" (`S453/batches.md:1053`); V6 RISK LOW: "C13's inputs/output/vm field can cross the 700 warn ceiling" (`:1158`). Confirmed `wc -l` = 700.                                                                                                                                                                                                    | Minor (warn-level lint)                  | **Covered by Batch 13 AC 1** (reports before/after count). Batch 13 will cross 700 (warn only). A facade-rule split (e.g. replay retention + motion hold collaborator) is a **separate follow-up after M2**, not inside Stage 2.                                                                                        | `chat-transcript.component.ts` + a new collaborator                                                             | M (split)                         | Yes                                                                  | Split touches the retention/effect code Batch 13 AC 5 freezes → only after Batch 15.                                          |
| B13 | Triple-copied transcript spec harness                                                | "the transcript spec harness is copied three times (`chat-transcript.component.spec.ts`, `.replay-motion.spec.ts`, `.replay-mount.spec.ts`)" (`S453/batches.md:780-781`; `b5-code-style-review-delta.md:72`) — accepted                                                                                                                                                                      | Minor (accepted)                         | **Separate follow-up after Batch 15.** Batch 13 creates a 4th copy (`chat-transcript.older-history.spec.ts`) and requires the 3 specs unedited (`:1536`).                                                                                                                                                               | the 4 transcript specs + a shared `testing/` harness file                                                       | S-M                               | Yes                                                                  | Direct conflict with Batch 13 AC 5 if done earlier.                                                                           |
| B14 | Unattributed rAF sites / no `GPUTask`                                                | "two unattributed minified rAF sites (14.4 %); no `GPUTask` in trace" (`S453/batches.md:841-842`)                                                                                                                                                                                                                                                                                            | Info                                     | **Backlog.** Re-check in M2 trace (Batch 15) only if AC-11 still fails.                                                                                                                                                                                                                                                 | —                                                                                                               | —                                 | —                                                                    | —                                                                                                                             |

Already closed (no action): A8 residual (checked in M1 rAF histogram, `S453/batches.md:782`,
`:815`); b7 logic Moderates (edge-tracker cross-reference and zero-tree queued-replay spec, closed in
Batch 7 revise round 1, `:1041-1045`); Task 3.2 AC 9 reconciliation (`S453/handoff.md:107-108`);
diagnostics write failures `console.warn` only (by design, AC-12, `S453/batches.md:339-341`).

## Recommended grouping

### (1) Fold into TASK_2026_453 on this branch (amend `batches.md`; team-leader decomposes)

- Batch 12: add B3 (compaction-reload ordering spec) and B4 (throw-mid-replay state spec) as ACs.
- Batch 13: add B11 (timer-clear dedupe) as the first step; B12 stays as AC 1 line-count report.
- Batch 14: add B6 (per-tile DOM sampling in `perf-page-capture.ts`, closes M1 AC 2 at M2); with it,
  B8 (`startTraceCapture` try/catch) and B7 (split `assertScrollSanity` if an export is added).
- Already covered, no change: B1 (Batch 8), B2 (U1 escalation triggers).

### (2) Separate small task/branch for CLI lanes (off `main`, not this branch)

- A3: narrow the `chore/bump-*` guard in `ci.yml:48`, `electron-e2e.yml:44`, `vscode-e2e.yml:56`
  to the 3 bot prefixes. S.
- A2 part 1 (FU-16c): align `file-settings-keys.ts:631` default to 2 and fix the stale doc at
  `internal-query-queue-timeout.error.ts:6`. S.
- A2 part 2 (FU-16b-c): implement the option the user picks. M.
- A4: write the property-hub load-test setup/cleanup scripts, if the user says yes. S.
- A1 optional: add a `dry_run` input to `publish-electron.yml` (skip tag, signing, release). M.
- After TASK_2026_453 Batch 15 (separate follow-up on `main`): B9 (Jest target for e2e perf
  helpers, needs `nx reset`), B12 split, B13 shared transcript spec harness, B10 rendered-class
  assertion.

### (3) Needs the user (manual or outward-facing)

1. **A1 / D10**: choose one — (a) run Sync Release Branch for `electron` (a real public release:
   tag, bump PR, GitHub Release, eSigner cost), or (b) approve the `dry_run` input first and then
   dispatch it, or (c) wait for the next normal release. Then confirm all 3 OS jobs are green.
2. **A2 / FU-16b-c**: pick an option: raise global to 3 with background capped at limit − 1;
   cap background without raising (regresses TASK_2026_352); or let `user-action` overshoot by one.
3. **A4**: say yes/no to the scripts; the load test run itself (clone, 3 streaming tiles, B-E
   actions, read logs) is manual and must not overlap a perf run.
4. **A5 / AC-10**: boot the dev app with `PTAH_PROFILE_ON_LAG_MS=1000`, open `D:\projects\ptah-437`,
   wait ~3 min, paste the log lines after `[Ptah Electron] Clipboard IPC registered` (all
   `[event-loop] lag` maxMs < 1000, governor + adopter lines, empty `ptah-hang.log`). Idle machine.
   Then close `S437/task.md` (`status:` line).
5. **B2 / U1**: approve an `onScroll` change only if a Batch 8 / M2 / Task 14.2 trigger fires.
6. Housekeeping: remove the `D:\projects\ptah-437` worktree; decide on PR #457 (`main` →
   `release/cli`, against the release-branch rule).

### (4) Drop as already done / accepted

- A3 root-cause question ("find out why #512 skipped"): answered — deliberate guard from `287dd7d23`;
  PR #512 is merged. Only the narrowing fix (group 2) remains.
- B5 FU-20a: accepted and documented, no fix planned.
- B14 unattributed rAF / no GPUTask: info only unless M2 fails.
- Closed items listed under Table B (A8 residual, b7 Moderates, Task 3.2 AC 9, diagnostics
  `console.warn`).
