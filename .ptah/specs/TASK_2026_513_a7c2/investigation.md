## Field sources

MEASURED: Source investigation on 2026-09-21, HEAD observed during investigation `3db2b8e619443b3a486cad9cebe48cb50435ae24`, including existing working-tree changes. Citations are relative to `D:\projects\ptah-extension`. `MEASURED` means inspected source or an executed probe; it does not mean the original session was observed. `INFERRED` marks explanations dependent on unobserved state and proposed changes. The original numbers and session identity are reported observations in `context.md`, not independently captured telemetry. Questions 1 and 2 are answered by this table and its transport chains.

| Field | Rendering component (file:line) | Store or signal (file:line) | Writer (file:line) | Scope of the value (session, turn or agent) |
| --- | --- | --- | --- | --- |
| MEASURED: MODEL | `libs/frontend/chat-ui/src/lib/molecules/session/session-stats-summary.component.ts:76`, `:85`; `primaryModelName` at `:713` | `TabManagerService._tabs`, keyed by `TabState.id`, at `libs/frontend/chat-state/src/lib/tab-manager.service.ts:165`; fields `liveModelStats.model`, fallback `modelUsageList[0].model`; tile selectors at `libs/frontend/chat/src/lib/components/templates/chat-view.component.ts:753`, `:760` | `libs/frontend/chat/src/lib/services/chat-store/session-stats-aggregator.service.ts:104`, `:115` calls setter `libs/frontend/chat-state/src/lib/tab-manager.service.ts:2010`; model originates in result projection `libs/backend/agent-sdk/src/lib/helpers/stream-transformer.ts:454`, `:501`. Loaded path: `libs/frontend/chat/src/lib/services/chat-store/session-loader.service.ts:862`, `:890` | Session-keyed latest usage/context metadata; not a spawned-agent record and not directly the selected session model. Incoming push is routed by `sessionId` to tabs bound through `claudeSessionId` (`session-stats-aggregator.service.ts:68`). |
| MEASURED: TOKENS | `libs/frontend/chat-ui/src/lib/molecules/session/session-stats-summary.component.ts:108`; `totalTokenCount` at `:782` | `summary()` at the same file `:757`: `TabState.preloadedStats.tokens` if present, otherwise sum of `TabState.messages[].tokens`; tile selectors `libs/frontend/chat/src/lib/components/templates/chat-view.component.ts:667`, `:746` | Backend result `usage` projected at `libs/backend/agent-sdk/src/lib/helpers/stream-transformer.ts:553`, `:576`; frontend pending/late-message writer `libs/frontend/chat-streaming/src/lib/streaming-handler.service.ts:597`, `:641`; finalization `libs/frontend/chat-streaming/src/lib/message-finalization.service.ts:160`, `:235`; loaded accumulation `libs/frontend/chat/src/lib/services/chat-store/session-stats-aggregator.service.ts:141` | Session aggregate over retained message/turn records, or preloaded aggregate plus subsequent stats. Display adds input + output + cacheRead, excluding cacheCreation. No external-agent usage input in this chain. |
| MEASURED: COST | `libs/frontend/chat-ui/src/lib/molecules/session/session-stats-summary.component.ts:117` | Same `summary()` at `:757`: `preloadedStats.totalCost` or summed `messages[].cost`; actual sum `libs/shared/src/lib/utils/subagent-cost.utils.ts:188` | Direct live SDK `total_cost_usd` at `libs/backend/agent-sdk/src/lib/helpers/stream-transformer.ts:538`; proxy recomputation at `:454`, `:465`, `:545`; push cost `:575`; message writer `libs/frontend/chat-streaming/src/lib/streaming-handler.service.ts:642`, or finalizer `libs/frontend/chat-streaming/src/lib/message-finalization.service.ts:236`; preload addition `libs/frontend/chat/src/lib/services/chat-store/session-stats-aggregator.service.ts:137` | Session aggregate of message costs or preloaded costs. Backend and frontend treat incoming stats as turn contributions, while the separate per-model breakdown is replaced. See cost caveats below. |
| MEASURED: TIME | `libs/frontend/chat-ui/src/lib/molecules/session/session-stats-summary.component.ts:119`, `:127` | `summary().totalDuration`, same file `:757`; sums `TabState.messages[].duration` in `libs/shared/src/lib/utils/subagent-cost.utils.ts:194` | SDK result `duration_ms` at `libs/backend/agent-sdk/src/lib/helpers/stream-transformer.ts:577`; pending/late writer `libs/frontend/chat-streaming/src/lib/streaming-handler.service.ts:600`, `:643`; finalizer `libs/frontend/chat-streaming/src/lib/message-finalization.service.ts:163`, `:237` | Sum of recorded message/turn durations, not time since tile creation. Preloaded branch forces duration to zero (`session-stats-summary.component.ts:763`) and hides TIME. |

MEASURED: The four-field bar is inside the tile's chat view, not the canvas title strip. `libs/frontend/canvas/src/lib/canvas-tile.component.ts:239` hosts `ChatViewComponent`; `:394` and `:399` provide tile-specific `SESSION_CONTEXT`. `libs/frontend/chat/src/lib/components/templates/chat-view.component.ts:708` resolves that tab; its template at `libs/frontend/chat/src/lib/components/templates/chat-view.component.html:27` binds messages, preloaded stats, live stats and model usage separately. The backing signal updates by tab id at `libs/frontend/chat-state/src/lib/tab-manager.service.ts:1077`, `:1084`. Thus a field's producer must be traced independently even though all four share a visual bar.

MEASURED: The common live transport is: `SdkAgentAdapter` supplies its registered result callback at `libs/backend/agent-sdk/src/lib/sdk-agent-adapter.ts:747`; `StreamTransformer` emits validated `{sessionId,cost,tokens,duration,modelUsage}` at `libs/backend/agent-sdk/src/lib/helpers/stream-transformer.ts:573`, `:583`; `wireResultStatsCallback` registers the backend receiver at `libs/backend/cli-agent-runtime/src/lib/wiring/sdk-callbacks.ts:135`, calls the sender at `:142`, and broadcasts `MESSAGE_TYPES.SESSION_STATS` at `:403`. That constant is `session:stats` (`libs/shared/src/lib/types/messages/message-constants.ts:132`). The renderer dispatches it at `libs/frontend/chat/src/lib/services/chat-message-handler.service.ts:142`, forwards payload at `:570`, and `ChatStore` delegates at `libs/frontend/chat/src/lib/services/chat.store.ts:511`. `SessionStatsAggregatorService` routes by session at `libs/frontend/chat/src/lib/services/chat-store/session-stats-aggregator.service.ts:68`, writes model metadata at `:115`, adds preloaded totals at `:141`, and delegates message usage at `:156`.

MEASURED: Message stats arriving before finalization are stored as `TabState.streamingState.pendingStats` (`libs/frontend/chat-streaming/src/lib/streaming-handler.service.ts:597`). `MessageFinalizationService` consumes them at `libs/frontend/chat-streaming/src/lib/message-finalization.service.ts:160`, places usage on the last new assistant message at `:215`, `:235`, and commits at `:249`. Stats arriving later overwrite the last assistant's fields at `libs/frontend/chat-streaming/src/lib/streaming-handler.service.ts:628`, `:639`, calling `TabManagerService.setMessages` at `:645`; the setter is `libs/frontend/chat-state/src/lib/tab-manager.service.ts:1548`. This record is session/tab keyed, then message-id keyed; the stats payload itself has no turn id (`streaming-handler.service.ts:517`).

MEASURED: The alternate loaded-session chain is `chat:resume` from `libs/frontend/chat/src/lib/services/chat-store/session-loader.service.ts:704`; RPC delegates at `libs/backend/rpc-handlers/src/lib/handlers/chat-rpc.handlers.ts:250`; `ChatSessionService` calls history at `libs/backend/rpc-handlers/src/lib/chat/session/chat-session.service.ts:805`, returns its stats at `:943`; `ChatHistoryReadService` calls `readSessionHistory` at `libs/backend/rpc-handlers/src/lib/chat/session/chat-history-read.service.ts:60`; `SessionHistoryReaderService` aggregates at `libs/backend/agent-sdk/src/lib/session-history-reader.service.ts:252`, with actual token/cost loops at `:1001`, `:1039`, return at `:1112`. Loader receives stats at `libs/frontend/chat/src/lib/services/chat-store/session-loader.service.ts:759`, applies them at `:770`, `:862`, and installs the context snapshot model at `:890`. `applyLoadedSessionStats` stores `preloadedStats`, `sessionModel`, and a provisional live model at `libs/frontend/chat-state/src/lib/tab-manager.service.ts:2042`, `:2065`.

INFERRED: If the screenshot used this source version, its visible nonzero TIME selects the message-summary branch, because the preloaded branch returns zero and suppresses TIME. This is a branch deduction, not a capture of the original tab state.

## Why the model badge is wrong

MEASURED: The badge consumes usage-derived model identity, not the session's provider/model selection: `primaryModelName` prefers `liveModelStats.model`, otherwise the first usage model (`libs/frontend/chat-ui/src/lib/molecules/session/session-stats-summary.component.ts:713`). `deriveLiveModelStats` uses a matching sticky `sessionModel` only when it appears in the usage list; otherwise it picks the highest-cost model (`libs/frontend/chat/src/lib/services/chat-store/session-live-stats.util.ts:65`, `:71`; ranking at `libs/shared/src/lib/utils/pick-primary-model.ts:47`). Context suppression can preserve older live metadata (`session-live-stats.util.ts:83`; `libs/frontend/chat/src/lib/services/chat-store/session-stats-aggregator.service.ts:121`). A list of two or more models shows a Models count rather than this single badge (`session-stats-summary.component.ts:76`, `:155`, `:704`). Ranking alone therefore does not establish that a mixed Claude/Codex list produced the reported single-model badge.

MEASURED: There is a concrete backend identity defect: session query construction supports an isolated provider profile, but result stats use global auth. The complete conditional mechanism is:

1. MEASURED: `SDK_AUTH_ENV` is a singleton object (`libs/backend/auth-providers/src/lib/di/register.ts:38`). A workspace Claude CLI profile instead creates its own native-auth environment with blank base URL (`libs/backend/auth-providers/src/lib/auth/workspace-provider-profile-resolver.ts:162`). `ChatSessionService` resolves the workspace profile at `libs/backend/rpc-handlers/src/lib/chat/session/chat-session.service.ts:543`.
2. MEASURED: `SdkAgentAdapter` extracts the profile environment at `libs/backend/agent-sdk/src/lib/sdk-agent-adapter.ts:674` and passes `authEnvOverride` into the query at `:727`. The query options choose it at `libs/backend/agent-sdk/src/lib/helpers/sdk-query-options-builder.ts:780`. The actual Claude query can therefore be independent of global provider configuration.
3. MEASURED: The following `streamTransformer.transform` call passes no auth override (`libs/backend/agent-sdk/src/lib/sdk-agent-adapter.ts:742`). `StreamTransformer` instead captures `this.authEnv` at `libs/backend/agent-sdk/src/lib/helpers/stream-transformer.ts:309`. It is the shared object, not that query's profile.
4. MEASURED: Global OAuth proxy selection writes a proxy base URL and activates provider tiers (`libs/backend/auth-providers/src/lib/auth/strategies/oauth-proxy.strategy.ts:243`, `:247`). `ProviderModelsService` applies main-agent mappings into shared auth at `libs/backend/auth-providers/src/lib/provider-models.service.ts:682`, `:701`. This is a global-provider writer; the native Codex spawn is not this writer.
5. MEASURED: Stats pass the SDK's raw model through `resolveForCost(model, authEnv)` (`libs/backend/agent-sdk/src/lib/helpers/stream-transformer.ts:454`). On a proxy environment, `ModelResolver` replaces a Claude tier with the environment's corresponding mapped model (`libs/backend/auth-providers/src/lib/auth/model-resolver.ts:39`, `:147`, `:151`, `:195`). The projected `modelUsage[].model` is the replacement (`stream-transformer.ts:501`), and the live transport/table above carries it to the badge.

MEASURED: A no-build, in-memory probe of the production `ModelResolver` produced `gpt-5.6-sol` for raw `claude-opus-4-6` under `{ANTHROPIC_BASE_URL:'http://127.0.0.1:12345', ANTHROPIC_DEFAULT_OPUS_MODEL:'gpt-5.6-sol'}`. Passing `{ANTHROPIC_BASE_URL:''}` preserved `claude-opus-4-6`. DI/catalog dependencies were stubbed, pricing returned null, and no agent was spawned. Probe method and evidence are recorded in `agent-output-model_resolution.md`. This verifies the conditional remapping mechanism, not the original incident.

INFERRED: Global/session auth mismatch is an evidence-backed candidate for the reported Opus-to-GPT badge. The original global auth environment, session profile and result payload were not captured, so the incident's exact trigger is **not established**. It would be incorrect to call the last-spawned-agent theory confirmed.

MEASURED: The bottom-right `Claude CLI` label is also weaker evidence than assumed: chat input reads global `AuthStateService.authMethodLabel` (`libs/frontend/chat/src/lib/components/molecules/chat-input/chat-input.component.ts:475`, rendered at `:410`), whose label comes from global `_authMethod` (`libs/frontend/core/src/lib/services/auth-state.service.ts:417`, `:426`). It is not a read of this session's provider profile. Model display formatting performs exact catalog-id lookup, then string formatting (`libs/frontend/chat-ui/src/lib/molecules/session/session-stats-summary.component.ts:854`; `libs/shared/src/lib/utils/pricing.utils.ts:561`); it does not select the latest spawned agent.

## Why the totals disagree with the footer

MEASURED: Question 6 has a qualified answer: the footer faithfully renders one message's stored data, but its absolute accuracy is not proved. It reads `message().tokens.input + message().tokens.output`, `message().cost`, and `message().duration` at `libs/frontend/chat/src/lib/components/organisms/message-bubble.component.html:162`, `:167`, `:170`. The token badge honors that scalar at `libs/frontend/chat-ui/src/lib/atoms/token-badge.component.ts:56`. In the live case, header and footer mostly share the **same message data**, with different aggregation and token definitions. They are not necessarily two independent backend accounting records.

| Comparison | Evidence and explanation |
| --- | --- |
| MEASURED: TOKENS | Header sums messages (`libs/shared/src/lib/utils/subagent-cost.utils.ts:182`) and displays input + output + cacheRead (`libs/frontend/chat-ui/src/lib/molecules/session/session-stats-summary.component.ts:782`). Footer displays only input + output for one message (`message-bubble.component.html:163`). Cache reads can dominate the header even without children. Neither displayed formula includes cacheCreation, although header tooltip exposes it (`session-stats-summary.component.ts:797`). |
| MEASURED: COST | Header sums message costs at `libs/shared/src/lib/utils/subagent-cost.utils.ts:188`; footer renders just the selected message cost. The header does not multiply its displayed token count by the displayed model's rate. Unknown message/agent costs can null the summary (`subagent-cost.utils.ts:188`, `:201`, `:208`). |
| MEASURED: TIME | Header sums every recorded message duration at `libs/shared/src/lib/utils/subagent-cost.utils.ts:194`; footer uses one duration. Header formatting turns 361000 ms into `6m 1s` (`session-stats-summary.component.ts:841`). There is no tile-start clock in that computation. The SDK duration may itself include tool/agent waiting, but the header does not separately add child process lifetimes. |

MEASURED: The task-folder `accounting-probe.cjs` transpiles the production `calculateSessionCostSummary` in memory and supplies **deliberately chosen synthetic messages** with no agent tree. Its executed output is header tokens `3800000`, cost `2.55`, duration `361000`; last-message tokens `3100`, cost `1.44`, duration `67000`; agent count `0`. These chosen fixtures demonstrate that the three reported discrepancies need neither external-agent aggregation nor a wrong price. They are **not** the original payloads and are **not** a plain-session UI reproduction.

MEASURED: Question 4 has three cost paths:

- MEASURED: Direct live Anthropic uses SDK `usage.costUSD` and `total_cost_usd` unchanged (`libs/backend/agent-sdk/src/lib/helpers/stream-transformer.ts:457`, `:537`). No Ptah price table selects this live total. Direct/proxy classification depends on auth base URL (`libs/shared/src/lib/utils/auth-env.utils.ts:3`), so the auth mismatch above can select the wrong branch.
- MEASURED: Proxy live stats use the **resolved** model id at `stream-transformer.ts:455`, shared hydrated pricing or `pricingProvider.getPricing(resolvedModel)` at `:462`, and `calculateMessageCost` at `:465`. `ModelResolver.resolveForCost` looks up that id at `libs/backend/auth-providers/src/lib/auth/model-resolver.ts:199`. The shared map begins at `DEFAULT_MODEL_PRICING` (`libs/shared/src/lib/utils/pricing.utils.ts:64`, `:122`), accepts runtime provider catalog rates (`libs/backend/auth-providers/src/lib/provider-models.service.ts:1124`), and fallback OpenRouter lookup lives at `libs/backend/auth-providers/src/lib/providers/openrouter/openrouter-pricing.service.ts:49`. Cost is input*inputRate + output*outputRate + cacheRead*cacheReadRate + cacheCreation*cacheCreationRate (`libs/shared/src/lib/utils/pricing.utils.ts:331`). Unknown rates return null. The bundled table does not contain `gpt-5.6-sol`; its actual rate would require runtime catalog evidence.
- MEASURED: Loaded stats calculate from each transcript model, also through the implicit-global resolver (`libs/backend/agent-sdk/src/lib/session-history-reader.service.ts:950`, `:961`, `:1099`). They aggregate native SDK agent transcripts too (`:1039`). This is distinct from native external Codex process records. Main-history totals start after the latest compaction boundary (`:978`); they should not automatically be called lifetime totals.

INFERRED: A wrong rate is possible under the measured auth mismatch, but the observed `$2.55` does not prove it. The actual cost branch, catalog row and precise raw values remain unverified. Also, `modelUsage` is described as cumulative whereas aggregate SDK tokens are treated as per-turn deltas (`libs/backend/agent-sdk/src/lib/helpers/stream-transformer.ts:569`); proxy total cost sums that modelUsage (`:545`) while frontend preloaded costs add incoming values (`libs/frontend/chat/src/lib/services/chat-store/session-stats-aggregator.service.ts:137`). This is a separate possible cumulative-versus-delta accounting problem needing real consecutive result frames; it is not confirmed as the incident's cause.

MEASURED: Footer fallback/replay paths prevent certifying it universally correct. Without pending result stats, finalization uses the complete event (`libs/frontend/chat-streaming/src/lib/message-finalization.service.ts:164`). Complete-event pricing uses global `resolveForCost` (`libs/backend/agent-sdk/src/lib/message-transform/assistant-message.transformer.ts:380`, `:384`); history replay does likewise (`libs/backend/agent-sdk/src/lib/helpers/history/session-replay.service.ts:244`, `:573`). A late stats event updates the last assistant without a turn identifier (`libs/frontend/chat-streaming/src/lib/streaming-handler.service.ts:628`). None of these observations proves that the reported footer was wrong; they limit the claim that it was right.

## Hypotheses

| Hypothesis from context.md | Verdict | Evidence |
| --- | --- | --- |
| 1. Header model comes from the latest spawned CLI agent / last agent writer. | REJECTED for the inspected path | MEASURED: Badge reads tab live/usage model (`session-stats-summary.component.ts:713`), written from `session:stats`; child model is stored separately in `libs/frontend/chat-streaming/src/lib/agent-monitor.store.ts:715`, `:743`. No last-agent selector is in the header chain. Global/session auth mismatch provides a different verified conditional mechanism; the incident trigger remains INFERRED. |
| 2. Header token and cost totals roll up external spawned CLI lanes. | REJECTED for the inspected external Codex lane path | MEASURED: Header summarizes messages/preloaded stats, not agent monitor records. Native Codex usage is a textual monitor segment (`libs/backend/cli-agent-runtime/src/lib/cli-agents/cli-adapters/codex-cli.adapter.ts:1111`); parent linkage changes only `cliSessions`/`lastActiveAt` (`libs/backend/agent-sdk/src/lib/session-metadata-store.ts:1009`). Native SDK subagent history inclusion is a separate CONFIRMED behavior (`session-history-reader.service.ts:1039`). |
| 3. Wrong model selects wrong cost rate and explains this cost. | UNTESTED for the reported cost | MEASURED: Proxy branch resolves model and selects its rate (`stream-transformer.ts:454`, `:462`); global/session mismatch can therefore misprice conditionally. Direct live cost bypasses the table (`:538`). INFERRED: Which path and rate produced the original `$2.55` is unknown; aggregate versus single-message scope already explains a difference. |
| 4. TIME measures a different interval from the footer. | CONFIRMED, with corrected interpretation | MEASURED: It sums recorded message durations (`libs/shared/src/lib/utils/subagent-cost.utils.ts:194`), while footer uses one (`message-bubble.component.html:170`). The particular suggestion of whole-tile wall-clock age is REJECTED by this implementation. |

## Intended behaviour question

MEASURED: Question 3's external-agent chain is separate from session accounting. `ptah_agent_spawn` forwards requested model and caller session at `libs/backend/vscode-lm-tools/src/lib/code-execution/mcp-core/protocol-dispatcher.ts:759`, `:767`, `:770`; namespace enrichment resolves the parent at `libs/backend/vscode-lm-tools/src/lib/code-execution/namespace-builders/agent-namespace.builder.ts:175`, passes the request at `:290`, `:299`; `AgentProcessManager` resolves model and writes `AgentProcessInfo` at `libs/backend/cli-agent-runtime/src/lib/cli-agents/agent-process-manager.service.ts:284`, `:293`, with independent `parentSessionId` and `model` at `:300`, `:303`, stores by `agentId` at `:496`, emits spawned at `:582`. Wiring broadcasts `AGENT_MONITOR_SPAWNED` (`libs/backend/cli-agent-runtime/src/lib/wiring/agent-events.ts:167`); frontend records `MonitoredAgent.model` (`libs/frontend/chat-streaming/src/lib/agent-monitor.store.ts:743`).

MEASURED: Child usage enters **that agent's output**, not the header's accounting record: Codex `turn.completed` produces an info segment at `libs/backend/cli-agent-runtime/src/lib/cli-agents/cli-adapters/codex-cli.adapter.ts:1104`, `:1111`; manager buffers it at `libs/backend/cli-agent-runtime/src/lib/cli-agents/agent-process-manager.service.ts:513`; `AgentOutputBuffer` forms an `agentId`-keyed delta at `libs/backend/cli-agent-runtime/src/lib/cli-agents/agent-output-buffer.service.ts:217`; manager emits at `agent-process-manager.service.ts:1369`; wiring broadcasts `AGENT_MONITOR_OUTPUT` at `libs/backend/cli-agent-runtime/src/lib/wiring/agent-events.ts:185`, `:187`. This Codex segment carries token counts, no dollar cost. Persistent parent `CliSessionReference` fields omit model/usage/cost (`agent-events.ts:394`); `addCliSession` updates only child references and last activity (`libs/backend/agent-sdk/src/lib/session-metadata-store.ts:1009`). No child usage is added to the parent accounting record in this measured path.

MEASURED: Native SDK subagents are different. History discovers `{sessionId}/subagents/agent-*.jsonl` or legacy agent files (`libs/backend/agent-sdk/src/lib/helpers/history/jsonl-reader.service.ts:783`, `:796`), loads them into `agentSessions` (`libs/backend/agent-sdk/src/lib/session-history-reader.service.ts:336`), and explicitly sums their usage (`:1039`). The live shared summary walks agent trees for agent count/breakdown, but does not add a separate child cost sum to message costs (`libs/shared/src/lib/utils/subagent-cost.utils.ts:197`).

MEASURED: Product docs describe header running totals and a footer including subagents (`apps/ptah-docs/src/content/docs/chat/cost-and-tokens.md:18`, `:19`), plus subagent cost in the session summary (`:49`). These references do not distinguish external CLI lanes from native SDK subagents. The same page's Copilot-zero-cost statement at `:27` conflicts with current published-rate policy (`libs/shared/src/lib/utils/pricing.utils.ts:54`), so it is not an exact current accounting specification.

INFERRED: The **external CLI lane roll-up product decision is open**. Existing code implements native SDK subagent inclusion in some paths, but does not establish a header spend contract for external `ptah_agent_spawn` lanes. Preserve aggregation until an explicit decision records session-only versus parent-plus-external-lanes. The evidence does not support attributing this incident's totals to an intentional external-lane roll-up.

## Proposed fix

INFERRED: Proposals only; no implementation. First preserve the existing aggregation boundary. The smallest coherent changes differ by field:

| Field | Proposed change and files it would touch |
| --- | --- |
| INFERRED: MODEL | Carry the session's effective auth snapshot from `libs/backend/agent-sdk/src/lib/sdk-agent-adapter.ts` into `StreamTransformConfig` in `libs/backend/agent-sdk/src/lib/helpers/stream-transformer.ts`; use it for result identity and direct/proxy classification, including resumed/reused sessions. Do not change global mappings as a workaround. To guarantee that MODEL means the session's model even when native subagents use other models, provide explicit session model identity to `libs/frontend/chat/src/lib/components/templates/chat-view.component.ts`, its `.html`, and `libs/frontend/chat-ui/src/lib/molecules/session/session-stats-summary.component.ts`; keep per-model usage as a breakdown. All frontend work belongs to the other lane. |
| INFERRED: TOKENS | Retain session aggregation; label its scope and cache-read inclusion. Pass full token usage to the footer and expose matching cache detail. Minimum presentation files: `libs/frontend/chat-ui/src/lib/molecules/session/session-stats-summary.component.ts`, `libs/frontend/chat/src/lib/components/organisms/message-bubble.component.html`, and possibly `libs/frontend/chat-ui/src/lib/atoms/token-badge.component.ts` if changing the displayed definition. Explicitly choose whether cacheCreation joins the displayed count; compact view already includes it. Do not subtract external-lane usage that the measured chain never added. |
| INFERRED: COST | Keep session total versus turn cost visibly distinct in the summary/footer. The session-auth fix above also corrects wrong direct/proxy classification and rate identity when its prerequisite holds. Carry the same context into `libs/backend/agent-sdk/src/lib/message-transform/assistant-message.transformer.ts` through `libs/backend/agent-sdk/src/lib/sdk-message-transformer.ts` for complete-event consistency. Capture consecutive provider result frames before making any cumulative-cost delta change; do not infer a rate from the badge. |
| INFERRED: TIME | Label as summed turn durations, rather than elapsed tile age; no arithmetic change is supported by this observation. Minimum file: `libs/frontend/chat-ui/src/lib/molecules/session/session-stats-summary.component.ts`. If TIME must survive reload/compaction, separately extend `PreloadedStatsPayload` in `libs/frontend/chat-state/src/lib/tab-state.types.ts`, relevant shared resume contract, backend history projection, and `session-stats-aggregator.service.ts`; that is larger than the observed mismatch requires. |

INFERRED: Loaded history needs the same provider provenance policy: pass persisted/resolved session identity through `libs/backend/rpc-handlers/src/lib/chat/session/chat-history-read.service.ts`, `libs/backend/agent-sdk/src/lib/session-history-reader.service.ts`, and `libs/backend/agent-sdk/src/lib/helpers/history/session-replay.service.ts`. Legacy sessions without provider metadata need an explicit fallback. A renderer-only label fix would leave their global-model resolution untouched.

INFERRED: Later regression coverage should pin all four fields for a no-agent Claude session and one spawning a different-vendor external lane, plus a native-Claude workspace profile under global proxy auth. Include live, resume, compaction, multiple tiles, and late stats. The native spawn must leave parent identity unchanged; the chosen product contract must state whether child spend is excluded or separately attributed.

## Risk of the fix

- MEASURED: Actual proxy sessions rely on model remapping (`libs/backend/auth-providers/src/lib/auth/model-resolver.ts:147`). INFERRED: Removing mapping globally to preserve Claude ids would break proxy model/rate attribution; pass session context instead.
- MEASURED: Full and compact tiles share `TabState` stats; compact totals include cacheCreation and select live/session model (`libs/frontend/chat/src/lib/components/molecules/compact-session/compact-session-card.component.ts:114`). INFERRED: Changing the shared token definition can make compact/full displays diverge unless both are covered.
- MEASURED: The shared summary is also used to preserve pre-compaction totals (`libs/frontend/chat/src/lib/services/chat-store/compaction-lifecycle.service.ts:558`). INFERRED: Changing arithmetic there could change carried cost/token baselines or double-count a resumed snapshot.
- MEASURED: `SessionStatsSummaryComponent` is also rendered by the harness builder (`libs/frontend/harness-builder/src/lib/components/harness-builder-view.component.ts:406`). Non-tab stats accumulate in `SurfaceSessionStatsRegistry` by `sessionId` (`libs/frontend/chat-state/src/lib/surface-session-stats.registry.ts:53`, `:104`), fed by the no-tab aggregator branch (`libs/frontend/chat/src/lib/services/chat-store/session-stats-aggregator.service.ts:94`, `:209`). INFERRED: A new required session-model input or revised scope label needs a non-tab policy; otherwise workflow panels lose identity or show misleading attribution.
- MEASURED: Loaded `sessionModel` is also retained for continuation (`libs/frontend/chat-state/src/lib/tab-manager.service.ts:2038`). INFERRED: Replacing it with a spending-ranked usage model could affect subsequent execution, not just the badge. Keep session identity separate from usage ranking.
- MEASURED: Stats can arrive before or after finalization (`libs/frontend/chat-streaming/src/lib/streaming-handler.service.ts:510`) and the broadcast retries (`libs/backend/cli-agent-runtime/src/lib/wiring/sdk-callbacks.ts:401`). INFERRED: Any new delta accumulation requires turn identity/idempotency; a cosmetic fix should not alter event ordering or add another accumulator.
- MEASURED: Auth profiles isolate query construction, whereas global provider changes mutate a shared object (`sdk-agent-adapter.ts:727`; `oauth-proxy.strategy.ts:243`). INFERRED: Capture an immutable effective snapshot per session; assigning a new global environment would affect unrelated sessions and leave concurrent-session races.

## Unverified

- MEASURED limitation â€” Question 5: **NOT REPRODUCED**. No running Electron plain-Claude session was created or inspected. Reproducing the actual Orchestra tile requires a running product surface with provider access; no build was run. The in-memory resolver and arithmetic probes are not an end-to-end reproduction.
- INFERRED: The original session used a native Claude workspace profile while global auth had an Opus-to-`gpt-5.6-sol` proxy mapping. This is a confirmed conditional code defect, but these incident prerequisites remain unknown.
- INFERRED: The original `$2.55` selected the wrong rate. Actual SDK payloads, runtime pricing map, rate provenance and global/session auth snapshots were not available. Published-rate estimates also are not proof of an invoice charge.
- INFERRED: The original footer's `$1.44` and `1m 7s` were accurate for its intended turn. No raw result or UI record was captured; fallback and late-stats behavior prevent asserting correctness solely from its appearance.
- INFERRED: The observed 3.8M were dominated by cached parent tokens. The source permits this and the synthetic fixture demonstrates it, but the original input/output/cache breakdown is unknown.
- INFERRED: The observed TIME came from the current message-summary branch. That follows if the running product matched this checkout; the deployed bundle/version was not inspected.
- INFERRED: Proxy cumulative model costs might be repeatedly accumulated as deltas. Source contracts indicate the risk, but consecutive real result frames and their cost reset semantics were not measured.
- MEASURED limitation: No exhaustive absence claim is made about all historical versions, provider adapters or external vendor accounting. Rejected hypotheses apply to the inspected native external Codex spawn/header chains. No product code, tests or configuration were edited; no install, build, reset, commit or other Git mutation was performed. `task.md` remains `status: backlog`.
