## Verdict

accept with fixes — findings 1, 3, and 4 are resolved, but the peer-name hardening remains incomplete because Unicode directionality/control characters can still alter the visible identity line.

## Finding status

| Original finding                                               | Status         | Evidence checked                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| -------------------------------------------------------------- | -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1. Peer-origin result bypasses result handling                 | fixed          | The peer branch now admits only `isUserMessage(msg) \|\| isReplayMessage(msg)` before inspecting `origin` (`libs/backend/cli-agent-runtime/src/lib/ptah-cli/helpers/ptah-cli-stream-loop.service.ts:226`). A peer-stamped result therefore reaches the result handler, whose turn completion is at `:504`. The regression cases at `libs/backend/cli-agent-runtime/src/lib/ptah-cli/helpers/ptah-cli-stream-loop.inbound-peer.spec.ts:195` assert success completion, usage/cost, error completion, and absence of false peer rendering. These tests are sensitive to the fix: under the reviewed pre-revision unconditional-origin branch, both fixtures take the peer branch and `continue`, so the completion, usage/cost, error, and no-`Message from` assertions fail. |
| 2. Sender-controlled name can impersonate a trusted identity   | fixed          | The visible qualifier and 48-code-unit cap are present at `libs/backend/cli-agent-runtime/src/lib/ptah-cli/helpers/ptah-cli-stream-loop.service.ts:50` and `:53`. `flattenPeerName` now removes Unicode `Cc` and `Cf` at `:99`, which covers the bidirectional overrides and isolates this row first reported, then strips every quote class and the Markdown structural characters. `emitOutput` encloses the sanitized label in a code span at `:241`, so a bare URL in a name cannot autolink. Covered at `libs/backend/cli-agent-runtime/src/lib/ptah-cli/helpers/ptah-cli-stream-loop.inbound-peer.spec.ts:291` (bidirectional controls), `:310` (Markdown link forgery), `:336` (bare URL neutralization) and `:364` (curly quotes).                                  |
| 3. Envelope stripping deletes a bare literal closing tag       | fixed          | Stripping now requires the opening match at `libs/backend/cli-agent-runtime/src/lib/ptah-cli/helpers/ptah-cli-stream-loop.service.ts:141` and the closing match at `:146`; otherwise the untouched trimmed body is returned. The bare trailing-tag and tag-only cases are pinned at `libs/backend/cli-agent-runtime/src/lib/ptah-cli/helpers/ptah-cli-stream-loop.inbound-peer.spec.ts:323` and `:341`.                                                                                                                                                                                                                                                                                                                                                                     |
| 4. Replay flag adds a second transport copy of every user turn | no code needed | The accepted stream-volume tradeoff is explicitly recorded in `batch-1-inbound-busy-lane.md:204` and again in its revision response at `:290`. The production change remains the original null-valued `replay-user-messages` flag at `libs/backend/cli-agent-runtime/src/lib/ptah-cli/ptah-cli-registry.ts:733`; no revision-specific behavior was added for this finding.                                                                                                                                                                                                                                                                                                                                                                                                  |

The narrower admission set still covers every user turn: `isUserMessage` accepts a non-replay `type: 'user'`, while `isReplayMessage` accepts a replay `type: 'user'` (`libs/backend/agent-sdk/src/lib/types/sdk-types/claude-sdk.types.ts:376`). Thus both a live peer user turn and the measured replay form reach the peer branch. The old non-user messages that cease being surfaced are result/system/assistant shapes with a peer origin; results are now handled correctly, while the others were never peer user turns and previously could only produce a misleading empty peer line.

The required uncached command passed: `npx nx run-many -t test -p @ptah-extension/cli-agent-runtime --skip-nx-cache` ran one project, 62 suites, and 937 tests (936 passed, 1 skipped). Jest also reported that one worker required forced exit after the tests.

## New findings

None beyond the residual identity-spoofing gap recorded under original finding 2.

## Claims I could not verify

- I did not run a live two-session CLI exchange, so I could not independently confirm that every supported CLI version stamps a real peer turn with `origin.kind === 'peer'`; the current code and fixtures cover the documented live and replay user-message shapes.
- I did not mutate the source back to the rejected revision to execute a red-before run. The new result fixtures are nevertheless directly sensitive to the gate: with the old unconditional origin check they `continue` before every assertion target in the result handler.
- Ptah diagnostics tooling was unavailable in this session, so there is no separate `ptah_get_diagnostics` result; the uncached Nx test target is the verification evidence.
