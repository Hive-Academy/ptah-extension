---
id: TASK_2026_267
status: backlog
type: feature
title: >-
  Stack Profile registry — multi-language New Project with first-class .NET,
  external plugin marketplaces, and unified language detection
description: >-
  New Project is hardwired to Nx + NestJS + Angular/React: the RPC force-enables
  `ptah-nx-saas`, the seed prompt names three TypeScript-only skills, and the
  intake offers no platform choice. Introduce a declarative StackProfile registry
  that binds detection signals, intake vocabulary, toolchain probes, scaffold
  commands and initializer skills per platform; generalize the plugin loader to
  install from external `.claude-plugin/marketplace.json` repos so Microsoft's
  MIT-licensed `dotnet/skills` reaches users always-current; and ship a thin
  `ptah-dotnet` glue plugin. Python and any later language become one descriptor
  plus one glue plugin.
---

# Stack Profile registry + first-class .NET

Machine-owned metadata carrier. Prose lives in `./context.md`.
