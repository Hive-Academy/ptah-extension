# TASK_2026_413 test report

## Final status

Batch 6R is fully green. Every accepted independent-review fix is covered, all four required unit-test projects pass, all six lint/typecheck projects pass, and the combined Electron gate passes 10/10 at 1200x800.

## Commands and evidence

| Command / scope                                                                                                                                                                                                                                                                                                                                       | Result                                                                                     |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| `npm ci --no-audit --no-fund`                                                                                                                                                                                                                                                                                                                         | Passed; 2,982 lockfile-pinned packages installed locally                                   |
| Focused Batch 1 typechecks                                                                                                                                                                                                                                                                                                                            | Passed                                                                                     |
| `nx test @ptah-extension/vscode-core` focused scratch review                                                                                                                                                                                                                                                                                          | 2/2 passed; verifies merge-base semantics and no repository mutation                       |
| `nx test @ptah-extension/rpc-handlers -- --runInBand`                                                                                                                                                                                                                                                                                                 | 2,728 passed, 31 skipped                                                                   |
| `nx test @ptah-extension/platform-core -- --runInBand`                                                                                                                                                                                                                                                                                                | 548 passed                                                                                 |
| `nx test @ptah-extension/platform-vscode -- --runInBand`                                                                                                                                                                                                                                                                                              | 190 passed                                                                                 |
| `nx test @ptah-extension/platform-electron -- --runInBand`                                                                                                                                                                                                                                                                                            | 247 passed                                                                                 |
| `nx test @ptah-extension/platform-cli -- --runInBand`                                                                                                                                                                                                                                                                                                 | 201 passed                                                                                 |
| `nx test @ptah-extension/git-ui -- --runInBand`                                                                                                                                                                                                                                                                                                       | 21/21 suites, 284/284 tests passed                                                         |
| `nx test @ptah-extension/chat -- --runInBand`                                                                                                                                                                                                                                                                                                         | 66 suites, 1,037 passed, 2 skipped                                                         |
| `nx test ptah-extension-webview -- --runInBand`                                                                                                                                                                                                                                                                                                       | 8/8 suites, 150/150 passed                                                                 |
| `npx nx run-many -t lint typecheck -p @ptah-extension/shared @ptah-extension/platform-core @ptah-extension/platform-vscode @ptah-extension/platform-electron @ptah-extension/platform-cli @ptah-extension/vscode-core @ptah-extension/rpc-handlers @ptah-extension/git-ui @ptah-extension/chat ptah-extension-webview ptah-electron-e2e --parallel=1` | Passed all 22 targets for 11 projects; lint warnings only                                  |
| `npx nx e2e ptah-electron-e2e -- --grep "historical branch review controls\|in-editor hunk action widget\|hunk revert dialog"`                                                                                                                                                                                                                        | 5/5 passed in 85.8s; real Electron app at 1200x800, no resize/widen helper                 |
| `git diff --check`                                                                                                                                                                                                                                                                                                                                    | Passed                                                                                     |
| Detached base `712478de8`: `npx nx e2e ptah-electron-e2e -- --grep "CX:120\|opens, switches, and closes independent diff tabs"`                                                                                                                                                                                                                       | 2/2 passed by exit 0 in 120.1s; proves regression rather than pre-existing failure         |
| `npx nx test @ptah-extension/git-ui -- --runInBand --testPathPatterns=git-dock.mount.spec.ts`                                                                                                                                                                                                                                                         | 1/1 suite, 3/3 tests passed, including zero-editor/multi-row mount regression              |
| `npx nx e2e ptah-electron-e2e -- --grep "Git dock"`                                                                                                                                                                                                                                                                                                   | 5/5 passed in 1.3m on exact rerun                                                          |
| `npx nx e2e ptah-electron-e2e -- --grep "historical branch review controls\|in-editor hunk action widget\|hunk revert dialog"`                                                                                                                                                                                                                        | 5/5 passed in 2.0m at the unchanged 1200x800 window                                        |
| `npx nx run-many -t test -p @ptah-extension/git-ui`                                                                                                                                                                                                                                                                                                   | Ran test for exactly 1 project; 21/21 suites and 285/285 tests passed                      |
| `npx nx run-many -t typecheck lint -p @ptah-extension/git-ui ptah-electron-e2e`                                                                                                                                                                                                                                                                       | Ran typecheck/lint for exactly 2 projects (4 targets); all passed, 0 errors, warnings only |

The Electron command also rebuilt the Angular renderer and Electron main/preload/workers successfully. The review scenario observed the actual RPC calls for review changes/file, editor launch, and branch checkout. The hunk scenarios use mouse clicks against the mounted Monaco widget and modal controls at the default window size.

## Batch 6 diagnostic evidence

- Before the fix, the two active-branch scenarios reproduced 0/2. Their accessibility snapshots showed the first changed row only and a valid diff tab/hunk toolbar stuck at `Loading diff editor...`.
- The same scenarios passed at detached base `712478de8`, establishing a Batch 1-5 regression.
- After adding the contract-valid empty `editor:detectTargets` fixture, both formerly failing cases passed as part of the final 5/5 Git dock run.
- The first post-fix full dock attempt was 4/5 because the first Electron process never produced a window within the existing 30-second fixture timeout. The four executed scenarios, including both targets, passed. An unchanged exact rerun passed 5/5; no timeout was raised.

## Warnings

The Batch 6 lint gate reported 10 warnings and 0 errors across the two projects: the existing git-ui max-lines warning, existing hunk-revert non-null warnings, existing task-visual unused-disable warnings, and existing empty fixture callback warnings (including `fixtures.ts:50`, unrelated to the new typed response). Nx/Electron also emitted existing `NO_COLOR`, outdated agent configuration, max-listener, and non-fatal embedder warmup warnings; none changed the final command exit statuses.

## Batch 6R

| Command                                                                                                                                                                              | Result                                                                                                                                                                                                                                                          |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `npx nx run-many -t test -p @ptah-extension/vscode-core @ptah-extension/rpc-handlers @ptah-extension/git-ui @ptah-extension/chat --parallel=1`                                       | Passed; Nx confirmed **4 projects**. vscode-core: 34 suites / 527 passed. rpc-handlers: 95 suites / 2,738 passed / 31 skipped. git-ui: 21 suites / 286 passed. chat: 66 suites / 1,037 passed / 2 skipped. Aggregate: **216 suites, 4,588 passed, 33 skipped**. |
| `npx nx run-many -t lint typecheck -p @ptah-extension/vscode-core @ptah-extension/rpc-handlers @ptah-extension/git-ui @ptah-extension/chat ptah-extension-webview ptah-electron-e2e` | Passed; Nx confirmed **6 projects** and all 12 targets succeeded. Lint emitted warnings only, with no errors.                                                                                                                                                   |
| `npx nx e2e ptah-electron-e2e -- --grep "Git dock\|historical branch review controls\|in-editor hunk action widget\|hunk revert dialog"`                                             | **10/10 passed** in 3.3m using one worker; BrowserWindow remained 1200x800. Electron main/preload/workers and Angular renderer rebuilt successfully.                                                                                                            |
| `git diff --check`                                                                                                                                                                   | Passed with no output.                                                                                                                                                                                                                                          |

Focused evidence before the aggregate gate: vscode-core 34/34 suites and 527/527 tests; git-ui 21/21 suites and 286/286 tests; rpc-handlers 95/95 suites, 2,738 passed and 31 existing skips. The new workspace-path matrix is included in rpc-handlers, and the malformed-target real-child mount test is included in git-ui.

The first exact four-project test attempt ran projects concurrently and failed only through unrelated Windows resource pressure: 5-second timeouts in setup, voice, and real-git specs. The deterministic `--parallel=1` rerun above executed the same four project targets and passed. Nx marked vscode-core and rpc-handlers flaky based on that retry history; no timeout values or assertions were changed.

Existing lint warnings remain outside Batch 6R, including max-lines warnings and pre-existing non-null/unused declarations. There are no lint errors. No DI token or composition-root registration changed, so an extra expected-resolvable smoke run was not required; the Electron e2e build and startup still reported its built-in DI verification as 5/5.

## Post-rebase gate

Status: **PASS** after one minimal rebase-interaction repair in test-only composition fixtures. The rebased `EditorRpcHandlers` and `ElectronFileOpenRpcHandlers` require `PLATFORM_TOKENS.FILE_SYSTEM_PROVIDER`, while the hand-built Electron and VS Code smoke containers did not register that existing production port. The initial smoke run therefore failed 3 resolution cases. Registrations were added at `apps/ptah-electron/src/di/container.smoke.spec.ts:128` and `apps/ptah-extension-vscode/src/di/container.smoke.spec.ts:121`; no production behavior changed.

| Command / scope                                                                                                                                                                                                                                                                                                                                                                                                               | Result                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `npx nx run-many -t test -p @ptah-extension/shared @ptah-extension/platform-core @ptah-extension/platform-vscode @ptah-extension/platform-electron @ptah-extension/platform-cli @ptah-extension/vscode-core @ptah-extension/rpc-handlers @ptah-extension/agent-sdk @ptah-extension/git-ui @ptah-extension/chat @ptah-extension/markdown @ptah-extension/core ptah-extension-webview --parallel=1`                             | Passed; Nx confirmed **13 projects**. Aggregate: **480 suites** (479 passed, 1 skipped), **9,641 passed**, **35 skipped**, 13 todo. Per project: shared 1,368; platform-core 548; platform-vscode 190; platform-electron 247; platform-cli 201; vscode-core 529; rpc-handlers 2,743 (31 skipped); agent-sdk 1,513 (2 skipped); git-ui 286; chat 1,050 (2 skipped); markdown 151; core 665; webview 150.                  |
| `npx nx run-many -t lint typecheck -p @ptah-extension/shared @ptah-extension/platform-core @ptah-extension/platform-vscode @ptah-extension/platform-electron @ptah-extension/platform-cli @ptah-extension/vscode-core @ptah-extension/rpc-handlers @ptah-extension/agent-sdk @ptah-extension/git-ui @ptah-extension/chat @ptah-extension/markdown @ptah-extension/core ptah-extension-webview ptah-electron-e2e --parallel=1` | Passed; Nx confirmed **14 projects** and **28/28 targets**. Lint warnings only; 0 errors.                                                                                                                                                                                                                                                                                                                                |
| `npx nx e2e ptah-electron-e2e -- --grep "Git dock\|historical branch review controls\|in-editor hunk action widget\|hunk revert dialog"`                                                                                                                                                                                                                                                                                      | First complete execution: **9/10 passed**; mouse-only hunk staging timed out waiting for the index. An isolated unchanged retry passed 1/1 at 1200x800. A second Nx wrapper attempt exited during dependency startup before Playwright emitted a test header. Running the same built Playwright selection directly then passed **10/10** in 3.1m at 1200x800. This was transient runner pressure, not a product failure. |
| `npx nx run-many -t test -p ptah-electron ptah-extension-vscode --parallel=1 -- --runInBand --testPathPatterns=container.smoke.spec.ts`                                                                                                                                                                                                                                                                                       | Before repair: Electron 6/8 and VS Code 25/26, all 3 failures reporting unregistered `Symbol(PlatformFileSystemProvider)`. After repair: Nx confirmed **2 projects**; Electron **8/8**, VS Code **26/26**, aggregate **34/34 passed**.                                                                                                                                                                                   |
| `git diff --check`                                                                                                                                                                                                                                                                                                                                                                                                            | Passed with no output.                                                                                                                                                                                                                                                                                                                                                                                                   |

The first parallel 13-project test attempt hit the unchanged platform-core performance benchmark's 30-second ceiling under host contention; the identical spec exists on `origin/main`, and the serial rerun passed. Earlier lint/typecheck attempts also ended without source diagnostics under the same process pressure; the authoritative serial run passed all 28 targets. No timeout or assertion was changed.

Batch 8c-1 classification: markdown (151 tests) and core (665 tests) passed, and both projects passed lint/typecheck. There are no Batch 8c-1 failures to hand back. The shared Nx lock was acquired before the first gate command, held through all Nx/Playwright activity, and removed afterward.

## Batch 7

Status: **PASS**. The required Nx lock was acquired before each Nx/Playwright run and removed immediately afterward.

| Command                                                                                                                                          | Result                                                                                                                                                                                                                                                                                                                                            |
| ------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `npx nx run-many -t test lint typecheck -p @ptah-extension/git-ui @ptah-extension/core @ptah-extension/chat ptah-extension-webview --parallel=1` | Passed; Nx confirmed exactly **4 projects** and **12/12 targets**. Aggregate: **127 suites, 2,173 passed, 2 skipped**. Per project: core 28 suites / 671 passed; git-ui 22 suites / 302 passed; chat 69 suites / 1,050 passed / 2 skipped; webview 8 suites / 150 passed. Lint: 29 warnings and 0 errors (core 11, git-ui 1, chat 17, webview 0). |
| `npx nx run-many -t lint typecheck -p ptah-electron-e2e --parallel=1`                                                                            | Passed; Nx confirmed exactly **1 project** and **2/2 targets**. Lint reported 9 existing warnings and 0 errors.                                                                                                                                                                                                                                   |
| `npx nx e2e ptah-electron-e2e -- --grep "git source-control rail"`                                                                               | **1/1 passed** in 9.8s. Real Electron window remained 1200x800; the dock was 700 px. The scenario proved collapse, expansion, drag to 200 px within the 160-480 range, and collapsed/width restoration after a full app restart.                                                                                                                  |
| `npx nx e2e ptah-electron-e2e -- --grep "git source-control rail\|Git dock\|historical branch review controls\|in-editor hunk action widget"`    | **9/9 passed** in 52.0s using one worker: 5 Git dock, 1 rail persistence, 1 historical-review, and 2 hunk-widget scenarios. Electron main/preload/workers and the Angular renderer built successfully.                                                                                                                                            |
| `git diff --check`                                                                                                                               | Passed with no output before report updates; repeated after the report updates below.                                                                                                                                                                                                                                                             |

An initial four-project run exposed only test-harness omissions for the new internal resize component and a strict DOM query type; both specs were corrected. A focused git-ui rerun then passed 22/22 suites. The first e2e draft expected a 700 px rendered dock while the independent workspace sidebar was open, producing a 487 px measurement; the test now closes that sidebar through its real UI control. No product geometry, timeout, BrowserWindow size, or dock configuration was relaxed.

## Batch 8a

Status: **PASS**, with one pre-existing failure outside the batch's ownership (`ptah-electron:typecheck`, detailed below and in `implementation-report.md`).

| Command                                                                                                                                                                                                                                          | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `npx nx run-many -t test lint typecheck -p @ptah-extension/shared @ptah-extension/platform-core @ptah-extension/platform-electron @ptah-extension/platform-vscode @ptah-extension/rpc-handlers ptah-electron ptah-extension-vscode --parallel=1` | Nx confirmed exactly **7 projects** (plus 30 dependency tasks). **Zero test failures; zero lint errors.** Aggregate: **259 suites, 5,783 passed, 31 skipped, 10 todo.** Per project: shared 56 suites / 1,368 passed; platform-core 30 / 548 (4 todo); platform-electron 16 / 247 (3 todo); platform-vscode 17 / 190 (3 todo); rpc-handlers 97 / 2,854 (31 skipped); ptah-electron 37 of 38 suites / 518 passed (1 suite + 4 tests skipped); ptah-extension-vscode 6 / 58. Lint: 40 warnings, 0 errors (shared 2, platform-core 8, platform-electron 4, platform-vscode 2, rpc-handlers 19, ptah-electron 4, vscode app 1) — **none in a file this batch created or changed**. Single failed task: `ptah-electron:typecheck`. |
| `npx nx run-many -t test lint typecheck -p @ptah-extension/rpc-handlers @ptah-extension/cli-engine --parallel=1`                                                                                                                                 | Passed; Nx confirmed exactly **2 projects** and all 6 targets. rpc-handlers 97 suites / 2,854 passed / 31 skipped; cli-engine 17 suites / 173 passed. Lint 21 warnings, 0 errors. `cli-engine` is verified because Batch 8a edits its `expected-absent.ts` and `rpc-surface.spec.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `npx nx run ptah-electron:test --skip-nx-cache -- --testPathPatterns=navigation-policy`                                                                                                                                                          | **1 suite / 24 tests passed**, proving the new `navigation-policy.spec.ts` is actually collected by the Electron suite rather than silently excluded.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `git diff --check`                                                                                                                                                                                                                               | Passed with no output.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

### Coverage added

`workspace-file-path.spec.ts` (extended): the pure form gate driven through the `platform` parameter (relative, drive-absolute, root-absolute, empty, NUL/C0/DEL, UNC, `\\?\`, `\\.\`, `//server`, drive-relative, root-relative, ADS, `C:\a.ts:12:3`, and the accepted POSIX `notes:12`); and `resolveLinkedFilePath` against real temp trees for relative/absolute hits, `..` traversal, the `ws` vs `ws2` sibling-prefix trap, unregistered hint, no-base, document-relative resolution, lazy worktree widening (asserting `listWorktrees` is **not** called on a lexical hit), directory with and without `allowDirectory`, not-found, the size-cap boundary on both sides, and four form-gate cases proving via injected spies that `realpath` and `stat` were never called.

`file-link-root-policy.spec.ts`: the deny-list per class on both the lexical path and a symlink whose realpath lands in `.ssh`, win32 case-insensitivity, posix directory case-sensitivity, the registered-root exemption, view-vs-external root sets, the cap asymmetry, and UNC worktree dropping.

`file-view-rpc.handlers.spec.ts`: UTF-8/BOM/UTF-16LE decode, binary, invalid UTF-8, growth past the cap after `stat`, six schema rejections, every failure reason mapped to fixed path-free copy, reason-only logging, `externalOpenAllowed` true/false, no external offer when no lexical path survived, and no transport rejection when the policy throws.

`file-rpc.handlers.spec.ts` (VS Code, new): relative + authorized root, relative + unregistered root with no external fallback, UNC/device refused before any policy call, in-root absolute with no confirm, out-of-root absolute confirmed showing the absolute path, cancel, deny-listed credential refused without a confirm, column and line-only cursor placement, directory reveal, and sanitized error copy.

`navigation-policy.spec.ts`: same-document hash/reload/query-only (R9), sibling and relative-resolved `file:` paths, parent escape, UNC, non-`file:` schemes, empty and malformed URLs, and the refusal of `file:`/`javascript:`/`data:`/custom schemes for `shell.openExternal`.

`editor-rpc.handlers.spec.ts` (extended): scope routing, lexical-not-realpath launch, refusal for deny-listed and directory results, default-scope isolation, and schema rejection of an unknown scope.

### Failures encountered and resolved

The first rpc-handlers run reported **8 failures, all in the new `file-link-root-policy.spec.ts`**, and no product defect. `jest.spyOn(os, 'homedir')` threw `TypeError: Cannot redefine property: homedir` — `node:os` exports are non-configurable. Replaced with a `jest.mock('node:os', ...)` module factory, which keeps the production class free of a test-only constructor parameter that would have let a caller widen its own authorized roots. One assertion was also a bad fixture: `/home/me/.SSH/id_rsa2` was denied by the `id_rsa*` **basename** rule, so it proved nothing about posix directory casing; the fixture is now `.SSH/notes.md`. The UNC-worktree test was additionally strengthened — it previously used a path that failed at `realpath` and so never reached the lazy widening it claimed to exercise. After these test-only corrections the suite passed 97/97.

### Pre-existing failure (not Batch 8a)

`ptah-electron:typecheck` fails with six errors in `apps/ptah-electron/src/config/build-artifact-gate.ts` (`Cannot find name 'describe'/'it'`, `Cannot find namespace 'jest'`). Proven independent of this batch: moving the new `navigation-policy.spec.ts` aside and re-running with `--skip-nx-cache` reproduces the identical six errors; the file is unmodified at HEAD; and `tsconfig.app.json` excludes `**/*.spec.ts`, so no spec added here entered that program. Left unfixed because it is outside this batch's file ownership.

## Batch 8b

Status: **PASS**. The required Nx lock was acquired before every Nx/Playwright invocation and removed immediately afterward.

| Command                                                                                                                                                        | Result                                                                                                                                                                                                                                                                                                                   |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `npx nx run-many -t test lint typecheck -p @ptah-extension/git-ui @ptah-extension/core @ptah-extension/chat ptah-extension-webview --parallel=4`               | Passed; Nx confirmed exactly **4 projects** and **12/12 targets**. Aggregate: **130 suites, 2,206 passed, 2 skipped**. Per project: core 28 suites / 671; git-ui 25 / 335; chat 69 / 1,050 passed / 2 skipped; webview 8 / 150. Lint reported 29 existing warnings and 0 errors (core 11, git-ui 1, chat 17, webview 0). |
| `npx nx run-many -t lint typecheck -p ptah-electron-e2e --parallel=1`                                                                                          | Passed; Nx confirmed exactly **1 project** and **2/2 targets**. Lint reported 9 existing warnings and 0 errors.                                                                                                                                                                                                          |
| `npx nx e2e ptah-electron-e2e -- --grep "read-only file tabs"`                                                                                                 | **1/1 passed** in 6.5s. At the real 1200x800 BrowserWindow and measured 699-701 px dock, opened text at line 2/column 7, opened Markdown preview, toggled Source, closed the tab, rendered the fixed outside-root refusal, and required explicit confirmation before exactly one external-link editor request.           |
| `npx nx e2e ptah-electron-e2e -- --grep "Git dock\|historical branch review controls\|in-editor hunk action widget\|hunk revert dialog\|rail" --reporter=line` | **12/12 passed** with one worker: 5 Git dock, 1 source-control rail, 1 historical review, 2 hunk revert, 2 hunk widget, and 1 pre-existing Tasks rail scenario also matched by `rail`. The BrowserWindow remained 1200x800.                                                                                              |
| `git diff --check`                                                                                                                                             | Passed with no output before report updates; repeated after the report updates below.                                                                                                                                                                                                                                    |

The first combined regression run exposed the new tab strip's accidental loss of the legacy diff close-button accessible name. A focused rerun reproduced the exact locator failure; the product restored `Close diff for <label>` while file tabs use `Close file <name>`. The focused legacy scenario then passed 1/1 and the full selection passed 12/12. Two unit selector expectations were updated to the restored contract; no assertion, timeout, window, or dock dimension was weakened.

## Batch 8c-2

Status: **PASS**, with one pre-existing Batch 8b e2e failure proven independent of this batch. The shared Nx lock was acquired before every Nx and Playwright invocation and removed after each one, including on failure. `npx nx reset` was never run.

| Command                                                                                                                                                                                                                  | Result                                                                                                                                                                                                                                                                                                                |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `npx nx run-many -t test -p @ptah-extension/chat @ptah-extension/chat-ui @ptah-extension/core @ptah-extension/markdown @ptah-extension/tasks-ui @ptah-extension/tribunal-panel ptah-extension-webview`                   | Passed. Header confirmed **7 projects**. Aggregate **170 suites, 3,111 passed, 2 skipped (3,113 total)**. Per project: core 28 / 670; markdown 5 / 152; chat-ui 25 / 153; chat 70 / 1,063 passed + 2 skipped; tasks-ui 17 / 588; tribunal-panel 16 / 333; webview 9 / 152.                                            |
| `npx nx run-many -t lint typecheck -p <same 7>`                                                                                                                                                                          | Passed. Header confirmed **7 projects**, 14/14 targets. **0 errors**, 37 pre-existing warnings (core 11, chat-ui 6, chat 17, tasks-ui 3; markdown, tribunal-panel and webview clean). Every warning is a `max-lines`, non-null-assertion, empty-function or unused-import notice in a file this batch did not create. |
| `npx nx run-many -t lint typecheck -p ptah-electron-e2e`                                                                                                                                                                 | Passed. Header confirmed **1 project**, 2/2 targets. 0 errors, 9 pre-existing warnings.                                                                                                                                                                                                                               |
| `npx nx e2e ptah-electron-e2e -- --grep "agent file links" --reporter=line`                                                                                                                                              | **3/3 passed** in 21.4s.                                                                                                                                                                                                                                                                                              |
| `npx nx e2e ptah-electron-e2e -- --grep "agent file links\|git source-control rail\|Git dock\|historical branch review controls\|in-editor hunk action widget\|hunk revert dialog\|read-only file tabs" --reporter=line` | **14 passed, 1 failed** in 2.7m. The single failure is Batch 8b's `read-only file tabs`; see below. Every 8c-2 scenario passed inside this selection.                                                                                                                                                                 |
| `git diff --check`                                                                                                                                                                                                       | Clean. Only CRLF advisories, no whitespace errors.                                                                                                                                                                                                                                                                    |

### What the `agent file links` e2e actually proves

At the real 1200x800 BrowserWindow, with the dock measured at 699-701 px and no window or dock resize anywhere in the spec:

- An assistant turn is streamed in through the real `chat:chunk` seam, so the links are rendered by the real marked extension and the real DOMPurify pass, not by a fixture.
- Inline code `` `src/inline.ts:4` `` produces no anchor (AC 20).
- With the dock hidden, clicking `[a](src/a.ts:12:3)` opens it, adds an `a.ts` tab, and the observed `file:viewContent` carries `workspaceRoot: 'C:\ptah-e2e-ws'` — the chat tab's workspace, resolved from the host marker, not a guess. Monaco's cursor lands at `{ lineNumber: 12, column: 3 }`, which is what proves line and column survived (AC 19, 22).
- `[b](C:\ptah-e2e-ws\docs\readme.md)` renders the markdown preview first, and the Source toggle reveals Monaco (AC 23).
- The `https://` link fires no `file:viewContent` and leaves the window URL unchanged (AC 20, 27).
- An `outside-roots` result renders the refusal, and `editor:openFile` with `scope: 'external-link'` is observed only AFTER the confirm dialog showing the absolute path is accepted — zero calls before it (R1, AC 25).
- `mainWindow.reload()` completes and returns to the same URL (A6/A7, D10). See deviation 4 in the implementation report for what this step deliberately does not claim.

### Failures found and fixed during verification — all mine, all in test setup

1. **`file-link-wiring.spec.ts`, 2 failures.** `NG0201: No provider found for MODEL_REFRESH_CONTROL`. `FileLinkRouterService` reaches `TabManagerService`, which injects that inverted-dependency token. Fixed by adding `provideModelRefreshControl()`, the same helper `app.config.ts` and `thoth-message-routing.spec.ts` use. No production change.
2. **`unit5-message-routing.spec.ts`, 6 failures.** `NG0201: No provider found for FILE_LINK_OPENER`, path `TasksStore -> FILE_LINK_OPENER`. Fixed by binding a stub opener rather than the real router, which would have defeated that spec's "no deferred component instantiated" premise. No production change.
3. **e2e dock width measured 487, not 700.** The Workspaces panel shares the row and squeezes the dock. `git-rail-collapse.spec.ts` and `file-view-tab.spec.ts` both close that panel before measuring, for the same reason; the spec now does too. This is not a dock resize — the dock's configured width is never touched.
4. **e2e asserted `line`/`column` on `file:viewContent`.** The contract does not carry them and is right not to; the spec was wrong. Corrected to assert path and root there, with the Monaco cursor as the line/column proof. Detailed in implementation-report deviation 3.
5. **e2e `locator.click()` on the http link timed out.** The click SUCCEEDS, then Playwright blocks waiting for the genuine `https://` navigation it schedules — which only happens because the link is correctly NOT intercepted. Driven through `page.mouse.click` at the anchor's box centre instead: the same real user input, without the navigation wait. No assertion was weakened; the spec still proves no read is issued and the URL is unchanged.

No production code was changed to make a test pass. The only production edits in this batch are the ones listed in the implementation report.

### Pre-existing failure, proven independent (NOT fixed)

`read-only file tabs` (`apps/ptah-electron-e2e/src/specs/git/file-view-tab.spec.ts:144`) fails waiting for a button named `Close readme.md`. The product at HEAD emits `Close file <name>` for a file tab (`git-dock.component.ts:165`), so the 8b spec is stale against the 8b product fix.

Proven without `git stash`: `git diff --stat HEAD` is empty for that spec file and for all of `libs/frontend/git-ui`, so this batch touches neither; and `git show HEAD:libs/frontend/git-ui/src/lib/git-dock/git-dock.component.ts` plus `git show HEAD:apps/ptah-electron-e2e/src/specs/git/file-view-tab.spec.ts` reproduce the same mismatch at the committed tree. Both files belong to Batch 8b, so this is reported rather than patched.
