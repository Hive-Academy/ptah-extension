# TASK_2026_360 — Batches

Executor: CLI agents via `ptah_agent_spawn` (user decision 2026-08-31: sub-agents disabled).
Orchestrator verifies each batch with typecheck + tests before the next starts.
File sets are disjoint between B2 and B3; B1 is a prerequisite for both.

| Batch | Scope                        | Files (owner)                                                                                                                                                                                                                                                                                      | Plan § | Status                                                                                                                 |
| ----- | ---------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------- |
| B1    | Shared contract              | `libs/shared/src/lib/types/execution/stream-background.ts`, `libs/shared/src/lib/type-guards/guards/exec.ts`, `libs/shared/src/lib/types/rpc/rpc-session.types.ts`, shared schemas file holding `SdkTurnEndedPayloadSchema`, `libs/shared/src/index.ts` (exports)                                  | 1      | IMPLEMENTED                                                                                                            |
| B2    | Backend producers            | `libs/backend/agent-sdk/**` (registry, tokens, register, index, hook handlers, transformers, transformer-helpers, stream-transformer), `libs/backend/rpc-handlers/src/lib/chat/streaming/chat-stream-broadcaster.service.ts`, `libs/backend/rpc-handlers/src/lib/handlers/session-rpc.handlers.ts` | 2      | IMPLEMENTED                                                                                                            |
| B3    | Frontend applier + deletions | `libs/frontend/chat-types/**`, `libs/frontend/chat-state/**`, `libs/frontend/chat-streaming/**`, `libs/frontend/chat/**`, `libs/frontend/chat-ui/**`                                                                                                                                               | 3      | IMPLEMENTED                                                                                                            |
| B4    | Verification                 | typecheck + run-many test on the 8 projects; specs listed in plan §4                                                                                                                                                                                                                               | 4      | DONE (automated) — see the final verification entry in the log. Manual UI verification from plan §4 still outstanding. |

| B5a | Review fix — backend (F2 rekey merge) | `libs/backend/agent-sdk/src/lib/helpers/session-turn-state.registry.ts` + spec | review F2 | IMPLEMENTED |
| B5b | Review fix — frontend (F1 stale-event guard, F3 failure/abort paths, F4 hard-deny scoping) | `libs/frontend/chat-streaming/**`, `libs/frontend/chat-state/**`, `libs/frontend/chat/**` | review F1, F3, F4 | IMPLEMENTED |
| B5c | Review fix — frontend (F3 gap: session-scoped local reset when `chat:abort` fails) | `libs/frontend/chat/src/lib/services/chat-store/conversation.service.ts` + spec | review F3 (explicit fallback allowance) | IMPLEMENTED |

## Log

- 2026-08-31 — plan written by orchestrator; three software-architect sub-agent runs hung and produced nothing.
- B1 DONE: libs/shared/src/lib/types/execution/stream.ts (`'turn_state'` added to `StreamEventType`), libs/shared/src/lib/types/execution/stream-background.ts, libs/shared/src/lib/type-guards/guards/exec.ts, libs/shared/src/lib/type-guards/guards/exec.spec.ts, libs/shared/src/lib/types/sdk-hook.schemas.ts, libs/shared/src/lib/types/sdk-hook.schemas.spec.ts (new), libs/shared/src/lib/types/rpc/rpc-session.types.ts; no barrel edits needed (execution/guards/schemas barrels already `export *`), typecheck OK, tests OK (1216 passed, 49 suites)
- B2 DONE: libs/backend/agent-sdk/src/lib/helpers/session-turn-state.registry.ts (new: `SessionTurnStateRegistry` + `toTurnStateEvent`), libs/backend/agent-sdk/src/lib/di/tokens.ts (`SDK_SESSION_TURN_STATE_REGISTRY`), libs/backend/agent-sdk/src/lib/di/register.ts (`registerSingleton` + `rekey` subscribed to `SessionIdResolvedCallbackRegistry`), libs/backend/agent-sdk/src/lib/helpers/index.ts, libs/backend/agent-sdk/src/index.ts, libs/backend/agent-sdk/src/lib/helpers/{stop,stop-failure,subagent-stop}-hook-handler.ts (optional registry injection → recordStop / recordFailure / applySnapshot), libs/backend/agent-sdk/src/lib/sdk-message-transformer.ts + message-transform/transformer-helpers.ts (`helpers.turnState`, threaded through `createIsolated`), message-transform/{stream-event,assistant-message}.transformer.ts (root `message_start` → prepended `generating`), message-transform/result-message.transformer.ts (`settleTurn` → one `turn_state`), message-transform/system-message.transformer.ts (`task_notification` → `applySnapshot`, appended), libs/backend/agent-sdk/src/lib/helpers/stream-transformer.ts (`isResultMessage` added to the transform condition), libs/backend/rpc-handlers/src/lib/chat/streaming/chat-stream-broadcaster.service.ts (registry injected; catch → forceIdle chunk + flush before CHAT_ERROR; finally → forceIdle if still generating, `clear` after teardown unless the record was replaced; keyed by the last event sessionId), libs/backend/rpc-handlers/src/lib/handlers/session-rpc.handlers.ts (`session:status` returns `{ isActive, isStreaming: phase === 'generating', turnState }`; `ChatStreamBroadcaster` injection dropped). Specs: session-turn-state.registry.spec.ts (new), result-message.transformer.spec.ts (rewritten), + new describes in stream-event / assistant-message / system-message transformer specs, stream-transformer.spec.ts (result turn_state yielded after message_complete), the three hook-handler specs, chat-stream-broadcaster.service.spec.ts, session-rpc.handlers.spec.ts; existing harnesses updated for the new constructor parameter. typecheck OK (2 projects), tests OK (agent-sdk 1324 passed, rpc-handlers 2550 passed; `Running target test for 2 projects`), lint 0 errors, host DI smoke specs (ptah-extension-vscode, ptah-electron) pass.
- B3 DONE: libs/frontend/chat-types/src/lib/chat-types.ts (`'sleeping'` in `SessionStatus`, `TabState.lastTurnStateRevision`), libs/frontend/chat-state/src/lib/tab-persistence.ts (`sleeping` coerced on restore; `lastTurnStateRevision` dropped by the projection AND by `sanitizeRestoredTab`), libs/frontend/chat-state/src/lib/tab-manager.service.ts (new intent method `applyTurnState` — one `updateTabInternal` write + spinner-set update + revision guard; `markTabAwaitingBackground` deleted; `applyFinalizedTurn` microtask no longer overwrites a backend-owned `awaiting-background`/`sleeping` status and skips entirely when a `generating` re-lit the tab in the same batch; `lastTurnStateRevision` reset in `resetTabToFresh` / `applyNewConversationDraft` / `applyNewConversationStreaming` / `applyResumingSession` because a fresh SDK query restarts the backend counter), libs/frontend/chat-streaming/src/lib/turn-state-applier.service.ts (new `TurnStateApplier`: tabId → `findTabsBySessionId` → `findTabBySessionIdAcrossWorkspaces` via `SessionId.safeParse`; terminal phase → `finalizeCurrentMessage(tab.id, isAborted)` FIRST + hard-deny agent marking moved here from the deleted stats safety-net → `applyTurnState` → liveness with the owning tab's workspace; no tab → liveness only, no warn), libs/frontend/chat-streaming/src/index.ts (export), libs/frontend/chat-streaming/src/lib/streaming-handler.service.ts (`isTurnStateEvent` intercept at the top of `processStreamEvent`; self-heal re-mark + `ABORTED_TERMINAL_REASONS` deleted; `routeBackgroundEvent` writes `streamingState` only; hijack paths keep `attachSession` + `setSessionId` only; `handleSessionStats` = stash `pendingStats` while `streamingState` exists, else merge onto the last assistant message and return `queuedContent`; `PermissionHandlerService` injection dropped), libs/frontend/chat-streaming/src/lib/accumulator-core.service.ts (`case 'turn_state'` → `skip`), libs/frontend/chat/src/lib/services/chat-store/turn-end-handler.service.ts (snapshot-only: `handleTurnEnded` stamps fields, `handleSubagentEnded` calls `onStopped` only for a `knownEntry` + stamps `pendingBackgroundTasks`, `handleTurnFailed` stamps reason + `handleChatError`; `MessageFinalizationService` injection dropped), libs/frontend/chat/src/lib/services/chat-message-handler.service.ts (all `liveness.mark*` in chunk/turnEnded/subagentEnded/turnFailed deleted; claimed-surface `turn_state` chunk → `TurnStateApplier.apply(event)`; `workspaceFor` kept — `/clear` and `handleChatError` still use it), libs/frontend/chat/src/lib/services/chat-store/session-liveness-reconciler.service.ts (`turnState` → synthetic `TurnStateEvent` → `TurnStateApplier.apply(event, tabId)`; else `isActive` → `markIdle`; `isStreaming` branch deleted; workspace = `tabManager.activeWorkspacePath ?? config.workspaceRoot`), libs/frontend/chat/src/lib/services/workspace-coordinator.service.ts (`reconcileRestoredTabs()` after the synchronous switch fan-out, detached), libs/frontend/chat/src/lib/components/molecules/chat-input/chat-input.component.ts (`sleeping` → input enabled), libs/frontend/chat/src/lib/components/organisms/tab-bar.component.ts (pill for `sleeping`, `[crons]` bound), libs/frontend/chat-ui/.../awaiting-background-indicator.component.ts (`crons` input; "Sleeping — N scheduled wakeup(s)" with raw schedules in `title`; `data-mode` attr). Specs: turn-state-applier.service.spec.ts (new), tab-manager.intent-mutators.spec.ts (`applyTurnState` table + revision guard + finalize-microtask interplay), tab-manager.cross-workspace.spec.ts (background-partition `applyTurnState`), tab-restore-sanitize.spec.ts (`sleeping`, revision dropped), tab-manager.persistence.spec.ts (revision never persisted), streaming-handler.service.spec.ts + streaming-handler.background.spec.ts (no re-mark from content, turn_state interception, stats routing), turn-end-handler.{service,background,spinner}.spec.ts (snapshot-only; spinner spec now drives the real `TurnStateApplier` against the real `TabManagerService`), chat-message-handler.service.spec.ts, session-liveness-reconciler.service.spec.ts (rewritten), chat-lifecycle.service.spec.ts + workspace-coordinator.service.spec.ts (applier / reconciler mocked, switch → reconcile), tab-bar / chat-input / awaiting-background-indicator specs (`sleeping`). `MessageSenderService` untouched. typecheck OK (5 projects), lint 0 errors (5 projects; warnings pre-existing), tests OK (`Running target test for 4 projects` — chat-types has no test target — 1683 passed: chat-state 306, chat-ui 98, chat-streaming 389 + 1 skipped, chat 890 + 2 skipped).
- B5a DONE: libs/backend/agent-sdk/src/lib/helpers/session-turn-state.registry.ts (`rekey` now MERGES a colliding real-id record via new module-level `mergeRecords`: revision baseline = max(from, to); an in-flight `generating` state + `generatingEmitted` wins from either side, else the canonical state is kept; canonical `stopSnapshot` / `failure` win, placeholder ones fill nulls; placeholder entry always deleted; blank / equal / unknown-placeholder ids still no-op; header "## Alias" paragraph rewritten to describe the merge), session-turn-state.registry.spec.ts (rekey block: collision T generating@1 + S idle@0-with-snapshot → merged generating@1 → settle awaiting-background @2 and `get(T)` undefined; canonical generating + placeholder snapshot → phase stays generating, settle consumes the snapshot; canonical snapshot/failure preferred over placeholder; no duplicate generating after a merge until settle; revision monotonic across rekey with no collision, with `to.revision > from.revision` and with `from.revision > to.revision`). typecheck OK, tests OK (1350 passed, 1 skipped, 82 suites), eslint 0 errors on both files.
- B5b DONE: F1 — libs/frontend/chat-types/src/lib/chat-types.ts (`TabState.lastTurnStateSessionId`, never persisted), libs/frontend/chat-state/src/lib/tab-persistence.ts (dropped by `NON_PERSISTED_TAB_KEYS` / `projectTabForPersist` / `sanitizeRestoredTab`, mirroring `lastTurnStateRevision`), libs/frontend/chat-state/src/lib/tab-manager.service.ts (new read-only `canApplyTurnState(tabId, sessionId, revision)` + private `acceptsTurnState`: (a) ownership — placeholder tab (`claudeSessionId` null) accepts any session, a bound tab accepts only `claudeSessionId === sessionId`; (b) revision compared only under the same `lastTurnStateSessionId`, a different session on a bound tab = restarted counter (accept), on a placeholder tab the plain comparison holds because the backend keeps the counter continuous across the rekey; `applyTurnState(tabId, state, sessionId?)` applies the same rule and stores the session; both fields reset wherever the revision was reset), libs/frontend/chat-streaming/src/lib/turn-state-applier.service.ts (acceptance filter on every resolved tab BEFORE finalization / hard-deny consumption / `applyTurnState` / liveness; rejected tabs dropped from `targets` with a `console.debug`; liveness with no surviving target: skipped entirely when a tab bound to the event session rejected the revision (stale replay), otherwise guarded by a bounded per-session `surfaceRevisions` map (256 entries, oldest evicted) — a foreign-session drop may still idle the OLD session id, which never touches the tab). F3 — libs/frontend/chat/src/lib/services/chat-store/chat-lifecycle.service.ts (`handleChatError` = `clearCompactionState` + `console.error` + `clearQueuedContentAndOptions` per target tab + `loadSessions`; `finalizeCurrentMessage`, `applyErrorReset`, `markTabIdle`, `sessionManager.setStatus('loaded')` REMOVED; `SessionManager` / `StreamingHandlerService` injections dropped; `lastTerminalReason` is stamped by `handleTurnFailed` before the call and by the ordered `failed` event — CHAT_ERROR carries no reason), libs/frontend/chat/src/lib/services/chat-store/conversation.service.ts (`abortCurrentMessage`: `setLastTerminalReason('aborted_streaming')`, queue restore, resumable-subagent bookkeeping KEPT; `finalizeCurrentMessage` (both the `StreamingHandlerService` path and the private `applyStatusErrorReset` one) and `markTabIdle` REMOVED; private helper + import deleted; no timer fallback). `MessageSenderService.wireAbortDispatch` (tab-close `chat:abort`) only fires the RPC — untouched. Writer audit (libs/frontend, non-spec): REMOVED — chat-lifecycle.service.ts `handleChatError` finalize + `applyErrorReset` (status 'loaded') + `markTabIdle` + `setStatus('loaded')`; conversation.service.ts abort finalize (`applyStatusErrorReset` status 'loaded') + `markTabIdle`. KEPT — message-sender.service.ts:424/:440 `markLoaded` (chat:start RPC failure / throw, local reset), :641-642 + :657-658 `markLoaded` + `markTabIdle` (chat:continue RPC failure / throw, local reset), :651 optimistic `markStreaming` + `markTabStreaming` on send (by design); compaction-lifecycle.service.ts:123 (compaction timeout) + :307 (compaction complete) `markTabIdle`; completion-handler.service.ts:65-66 `applyStatusErrorReset` + `markTabIdle` (legacy, exported but NO production caller); tab-manager.service.ts `status: 'loaded'` intents — `setBackendEncodedPath` (setup), `markLoaded` (primitive), `reconcileUserMessageNativeUuid` / `applyFinalizedHistory` / `applyResumedHistory` (history), `applyFinalizedTurn` microtask (finalization, yields to backend-owned status), `clearStreamingForLoaded` (dedup), `applyErrorReset` / `applyStatusErrorReset` (primitives; `applyErrorReset` now has no production caller), `detachSessionAndMarkLoaded` / `applyResumeFailure` (resume local reset), `applyCompactionTimeoutReset` ×2 (compaction), `setNameAndTitle` (setup), `duplicateTab` (duplication); `SessionLivenessRegistry.markStreaming` is the registry method itself. F4 — libs/frontend/chat-streaming/src/lib/permission-handler.service.ts (`_hardDenyToolUseIds` is a `Map<sessionId, Set>`; bucket = `request.sessionId`, else the routed tab's (`request.tabId` → `_promptTargetTabs` → active tab) `claudeSessionId ?? tabId`; `consumeHardDenyToolUseIds(sessionId)` drains only that bucket); the applier consumes the bucket for `event.sessionId ?? tab.claudeSessionId ?? tab.id` only for accepted targets on terminal phases, once per bucket per event. Docs: chat-streaming/CLAUDE.md guideline 9 and chat-state/CLAUDE.md guideline 8 extended. Specs: turn-state-applier.service.spec.ts (`canApplyTurnState` mirrored in the mock; check-before-finalize order; stale foreign `idle@6` after `generating@1` → no finalize / consume / status / new-session liveness; delayed probe `idle@2` after `generating@3` → rejected, no liveness; placeholder accepts; bound-and-matching accepts; fan-out keeps only accepting tabs; surface revision guard per session; placeholder hard-deny bucket fallback; 3-arg `applyTurnState` assertions), tab-manager.intent-mutators.spec.ts (`canApplyTurnState` table incl. restarted counter for a newly bound session, foreign-session rejection on a bound tab, `resetTabToFresh` clears both fields), tab-manager.cross-workspace.spec.ts (passes the bound session id like the applier does), tab-manager.persistence.spec.ts + tab-restore-sanitize.spec.ts (`lastTurnStateSessionId` never persisted / dropped on restore), chat-lifecycle.service.spec.ts (`handleChatError` surfaces + drops queue, never finalizes / writes status / clears spinner / `setStatus`), conversation.service.spec.ts (abort RPC success → no `markTabIdle` / `applyStatusErrorReset` / `markLoaded`, tab stays streaming with its `streamingState`), permission-handler.service.spec.ts (per-session buckets: consuming B leaves A; accumulation; empty bucket; tab-session fallback). typecheck OK (5 projects), lint 0 errors (4 projects), tests OK (`Running target test for 4 projects` — 1705 passed, 3 skipped: chat-state 316, chat-ui 98, chat-streaming 401 + 1 skipped, chat 890 + 2 skipped).
- FINAL VERIFICATION (post-B5c, whole-task re-run): typecheck 7/8 pass — the single failure is `@ptah-extension/rpc-handlers` with 4 errors, ALL in files TASK_2026_360 never touched: `setup-rpc.handlers.ts:378` (`AnalysisRunV3` missing `version` / `runId` / `updatedAt` / `lifecycle`) and `wizard-generation-rpc.handlers.ts:584,618,848` (`generatedCount` not in `GenerationCompletePayload`). These belong to TASK_2026_361 (setup-wizard resumable analysis), which changed the checkpoint types in `libs/backend/agent-generation/**` + `libs/shared/.../types/wizard/phase.ts` in the same working tree without yet updating those two consumers. Tests: shared 1216, chat-state 316, chat-ui 98, chat-streaming 401 (+1 skipped), agent-sdk 1354 (+1 skipped), chat 890 (+2 skipped) — all green; rpc-handlers 2406 passed / 86 of 91 suites, the 5 failing suites (`setup-rpc.handlers`, `wizard-generation-rpc.handlers`, `resolve-handler-plan`, `output-style-rpc.handlers`, `rpc-allowlist`) all fail to COMPILE against the same TASK_2026_361 type drift. This task's two rpc-handlers specs (`chat-stream-broadcaster.service.spec.ts`, `session-rpc.handlers.spec.ts`) are among the 86 that pass.
- CLEARED + COMMITTED: TASK_2026_360 committed as `c2be5c34a` (81 files, staged by explicit path so nothing from TASK_2026_361/362/363 was swept in). Immediately afterwards TASK_2026_361 landed the missing consumer updates in the same working tree, and the drift above is gone: `nx run-many -t typecheck` over all 8 projects passes, and `@ptah-extension/rpc-handlers` tests are 2612 passed / 31 skipped across 91 of 91 suites. No code change was needed on this task's side — the failures were never ours.
- B5c DONE: libs/frontend/chat/src/lib/services/chat-store/conversation.service.ts (`abortCurrentMessage`: the owning tab id is captured BEFORE the `chat:abort` RPC; the RPC is awaited via `.catch(() => null)` so a transport failure and a `{ success: false }` result both call the new private `idleAbortedTabLocally(tabId, sessionId)`, which re-resolves the tab by id and returns without touching it unless `claudeSessionId === sessionId` still holds, then `messageFinalization.finalizeCurrentMessage(tabId, true)` only when the tab has a `streamingState`, then `tabManager.markTabIdle(tabId)`; the success path is unchanged — no finalize / `markTabIdle`, the ordered `turn_state` owns it; no timer; JSDoc documents success = ordered event owns the lifecycle, failure = no stream exists to emit one so the session-scoped local reset is the only thing that can clear the spinner), conversation.service.spec.ts (`MessageFinalizationService` now mocked in the TestBed; success test additionally asserts `finalizeCurrentMessage` not called; new `failure-path fallback` describe: `{ success: false }` → finalize(aborted) + `markTabIdle`; RPC rejects → same; session replaced on the tab during the in-flight RPC → no finalize / no `markTabIdle`; no `streamingState` → `markTabIdle` only). typecheck OK (@ptah-extension/chat; one transient failure from another agent's in-flight `chat-streaming`/`chat-types` edits cleared on rerun), tests OK (890 passed, 2 skipped, 59 suites; conversation.service.spec 12 passed).
