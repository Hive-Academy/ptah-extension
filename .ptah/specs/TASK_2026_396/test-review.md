## Verdict

REJECT — the three new tests satisfy AC1/AC2/AC3'/AC6 and fail against the reverted probe, but no test pins the task's intended clean failure when a vendor rejects a stale session id, and the other system-CLI values remain unprotected against vendor-specific gating.

## Per-test judgement

| Test name | Fails on revert | Carries both fs assertions | Verdict |
|---|---|---|---|
| `resumes a codex session when the claude projects directory is absent` | Yes | Yes | Valuable: the reverted `readdir` rejects before spawn, so `result.success` at `agent-rpc.handlers.resume-parent-session.spec.ts:480` becomes false and the spawn assertion at line 481 fails. It pins the probe-free route into `agentProcessManager.spawn` at `agent-rpc.handlers.ts:800-801`, not the `resumeSessionId` assignment itself. |
| `passes the codex thread id through as resumeSessionId when the session file is missing` | Yes | Yes | Valuable: the reverted lookup reaches the rejected `access` configured at `agent-rpc.handlers.resume-parent-session.spec.ts:492-495` and never spawns. Independently, the exact assertion at lines 504-506 pins `resumeSessionId: params.cliSessionId` at `agent-rpc.handlers.ts:804`. |
| `threads the cli session id into both halves of the ptah-cli resume` | Yes | Yes | Valuable: the reverted lookup hits the rejected `readdir` at `agent-rpc.handlers.resume-parent-session.spec.ts:516-518` before either half spawns. The two exact assertions at lines 531-536 pin `agent-rpc.handlers.ts:878` and `agent-rpc.handlers.ts:910`. |

No new test is worthless by the stated mutation criterion: all three fail when the complete production fix is reverted.

## Findings

1. **MEDIUM — Stale-session failure behavior is untested.** The harness makes both resume collaborators succeed unconditionally: `spawnAgent` resolves a handle at `agent-rpc.handlers.resume-parent-session.spec.ts:91-95`, while `spawnFromSdkHandle` and `spawn` resolve agent ids at lines 99-102. Every new test consequently asserts success (`:480`, `:503`, `:530`). Production has two distinct owned-failure paths — a Ptah registry failure is converted to an exception at `agent-rpc.handlers.ts:883-885`, and all thrown failures are converted to `{ success: false, error }` at lines 818-825 — but neither is pinned here. Concrete scenario: Codex rejects a stale thread id from `agentProcessManager.spawn`; a regression that leaks that rejection or incorrectly reports success would leave all three new tests green.

2. **LOW — Five non-Codex system CLI values are not covered by the no-probe/pass-through assertions.** The new system-CLI cases use only `cli: 'codex'` at `agent-rpc.handlers.resume-parent-session.spec.ts:476` and `:499`. The current source of truth actually lists five others — `copilot`, `cursor`, `antigravity`, `opencode`, and `pi` — at `libs/shared/src/lib/types/agent-process.types.ts:62-69`. They currently share the generic branch at `agent-rpc.handlers.ts:800-808`, so the Codex case covers today's structure, but a reintroduced vendor-specific probe or id drop for any of those values would not fail this file. A parameterized test over all system CLI values would close that gap.

## Answers

1. **Revert sensitivity.** All three fail on a full revert of the production fix. Test 1 fails because the old unguarded directory read rejects, producing `{ success: false }` and no process spawn; it pins the direct, probe-free transition at `agent-rpc.handlers.ts:800-801` and the success return at line 817. It does **not** pin line 804: changing only `resumeSessionId` back to `undefined` would leave Test 1 green. Test 2 fails because its matched directory plus rejected access prevents spawn under the old code; its exact id assertion pins line 804. Test 3 fails because its rejected directory read prevents both Ptah halves under the old code; its assertions pin lines 878 and 910. None passes both before and after the complete fix.

2. **Reintroduced filesystem probe.** Yes. Test 1 has both negative assertions at `agent-rpc.handlers.resume-parent-session.spec.ts:482-483`; Test 2 has both at lines 507-508; Test 3 has both at lines 537-538. Any reintroduced probe using these `fs/promises` functions on the exercised paths fails even if its errors are caught.

3. **Mock hygiene.** There is no ordering dependency or cross-test configuration leak. The new block resets implementation and call history for both module-level mocks before every test at `agent-rpc.handlers.resume-parent-session.spec.ts:460-463`, and each test configures the failure state it needs. The preceding blocks have their own resets at lines 170-175, 241-248, and 374-378; the new block's first reset clears the immediately preceding block's successful implementations. `makeHarness()` constructs fresh RPC, registry, and process-manager mocks on every invocation at lines 87-138, and every test calls it locally (`:469`, `:491`, `:515`); no harness is shared.

4. **`resumeRaw`.** It is the correct helper. `makeHarness()` calls `handlers.register()` at `agent-rpc.handlers.resume-parent-session.spec.ts:136`, and `resumeRaw` submits a real `agent:resumeCliSession` message through `rpcHandler.handleMessage` at lines 143-153. The registered production callback executes `AgentResumeCliSessionParamsSchema.safeParse(params)` before branching at `agent-rpc.handlers.ts:763-775`. The helper therefore exercises, rather than bypasses, the Zod boundary.

5. **Rewritten test.** The new resume assertions are correct: the Ptah registry and process metadata must both retain `CLI_SESSION_ID`, matching production at `agent-rpc.handlers.ts:878` and `:910` and AC3'. Its name broadly describes the counterfactual it exercises: with the prior probe, the configured directory value `D-ws` at `agent-rpc.handlers.resume-parent-session.spec.ts:174` does not match the escaped `D--ws` form documented at lines 244-247, so the lookup would return false; the rejected `mockAccess` at lines 195-197 would not itself be reached. Parent-session coverage was not weakened: the rewritten test still pins the registry parent at line 203, and the immediately preceding test pins the same parent on both halves at lines 185-190. The rewritten test alone does not assert the process-manager parent, but neither did its prior form; the file's header promise remains covered by the pair.

6. **Coverage gaps.** The five other current system CLIs are not individually exercised; although they share the generic branch today, vendor-specific regressions would escape. The no-configured-`ptah-cli` branch is already covered at `agent-rpc.handlers.resume-parent-session.spec.ts:437-449`, including the actionable error and absence of registry spawn, so that is not a gap. No test supplies a stale id and makes either `agentProcessManager.spawn` reject or `PtahCliRegistry.spawnAgent` return its failure shape, so no test pins a clean `{ success: false, error }` response instead of a crash or false success. The focused suite was also executed during review and passed: 92 suites, 2,697 tests passed, 31 skipped.
