**REJECT**

1. **BLOCKER — §3–4: A post-commit error can leave the worker behind durable state.**  
   Evidence: [commit-store.ts:354](/D:/projects/ptah-extension/libs/backend/platform-electron/src/implementations/electron-state-storage-commit-store.ts:354) publishes `CURRENT`; verification can subsequently throw at `:411–419`. [worker-runtime.ts:481](/D:/projects/ptah-extension/libs/backend/platform-electron/src/implementations/electron-state-storage-worker-runtime.ts:481) updates its manifest only after commit returns. Keeping that worker alive lets its next write reuse the already-published generation and overwrite files referenced by `CURRENT`. The plan’s “batch failure means no data change” guarantee is false.  
   **Fix:** distinguish pre-publication failure from uncertain/post-publication failure. Reconcile durable state before accepting another request, or retire the runtime. Test a post-rename exception **followed by another mutation**, not merely reopening after a crash.

2. **MAJOR — §4, §7, §9: Existing merge semantics break both stdout placement and numeric cursors.**  
   Evidence: [worker-runtime.ts:156](/D:/projects/ptah-extension/libs/backend/platform-electron/src/implementations/electron-state-storage-worker-runtime.ts:156) rebuilds sequences by tag and chooses arrays by count. [agent-events.ts:406](/D:/projects/ptah-extension/libs/backend/cli-agent-runtime/src/lib/wiring/agent-events.ts:406) still includes inline segments. Consequently, saving `[segment, event, stdout]` and then normalizing its reference can produce `[segment, stdout, event]`; `tail: 1` no longer finds stdout. A longer incoming segment array can discard the stdout item entirely. Numeric paging at [worker-runtime.ts:567](/D:/projects/ptah-extension/libs/backend/platform-electron/src/implementations/electron-state-storage-worker-runtime.ts:567) cannot detect this reorder.  
   **Fix:** merge ordinary segments separately from stdout, preserve stdout explicitly, and define one canonical placement. Any non-append rewrite needs generation-bound cursors and corresponding frontend reset handling. Test the complete bulk-write → reference-write → read sequence.

3. **MAJOR — §4, §7: “Prefer longer” is unsafe for stdout tail windows.**  
   Evidence: [agent-process-manager.service.ts:1089](/D:/projects/ptah-extension/libs/backend/cli-agent-runtime/src/lib/cli-agents/agent-process-manager.service.ts:1089) retains only the last `MAX_STDOUT_PERSISTENCE_SIZE` characters. Two successive windows can have equal lengths and different content; neither contains the other. The cited precedent at [session-metadata-store.ts:577](/D:/projects/ptah-extension/libs/backend/agent-sdk/src/lib/session-metadata-store.ts:577) compares array lengths, not string coverage. Choosing one string and stripping the source loses reachable historical content.  
   **Fix:** deduplicate identical content only; discard a differing snapshot only with proven containment. Preserve other variants and explicitly identify the preferred read snapshot. Test equal-length, shifted, and non-overlapping tails.

4. **MAJOR — §6–7, §9–10: Restore can permanently memoize missing output before maintenance runs.**  
   Evidence: [session-metadata-store.ts:1008](/D:/projects/ptah-extension/libs/backend/agent-sdk/src/lib/session-metadata-store.ts:1008) reads references without migrating them. The plan removes their stdout immediately, while conversion runs after readiness. An early lazy read therefore finds no stdout marker and memoizes empty output—even after maintenance subsequently succeeds. Missing-id references never migrate. Sync adapters additionally lack `readJsonSequence`, as shown by [cli-state-storage.ts:16](/D:/projects/ptah-extension/libs/backend/platform-cli/src/implementations/cli-state-storage.ts:16); no sync implementation of the proposed stdout reader is specified.  
   **Fix:** provide a bounded worker-side legacy projection or on-demand conversion before serving lean restore data, plus an explicit sync fallback. Do not memoize migration-pending absence. Test reads before, during, and after the sweep, including legacy sync records.

5. **MAJOR — §4, §6–7, §10: Paged transport still hydrates fat details on Electron main.**  
   Evidence: [worker-host.ts:549](/D:/projects/ptah-extension/libs/backend/platform-electron/src/implementations/electron-state-storage-worker-host.ts:549) reconstructs complete values, including whole-string allocations. [session-metadata-store.ts:673](/D:/projects/ptah-extension/libs/backend/agent-sdk/src/lib/session-metadata-store.ts:673) returns the entire detail; [chat-session.service.ts:854](/D:/projects/ptah-extension/libs/backend/rpc-handlers/src/lib/chat/session/chat-session.service.ts:854) reads it before restore. Reusing this assembler merely distributes a multi-megabyte hydration across messages. The proposed 16 MiB ceiling still permits substantial inline output on main, repeatedly during saves and page authorization.  
   **Fix:** introduce worker-side metadata/reference projections and membership checks that omit bulk fields before transport. Measure main-thread allocations and received content, not only individual message sizes.

6. **MAJOR — §4, §6: Maintenance lacks a terminating, downgrade-safe progress model.**  
   Evidence: [worker-runtime.ts:673](/D:/projects/ptah-extension/libs/backend/platform-electron/src/implementations/electron-state-storage-worker-runtime.ts:673) deliberately retains missing-id output. Under the proposed selection rule, those unchanged records remain eligible forever and can starve later records. A source-plus-destination exceeding 16 MiB likewise has no specified progress path. Conversely, a permanent receipt skips all future scans, including after an old writer reintroduces inline stdout through [agent-events.ts:399](/D:/projects/ptah-extension/libs/backend/cli-agent-runtime/src/lib/wiring/agent-events.ts:399).  
   **Fix:** use a resumable key scan with explicit retained/oversized outcomes and guaranteed advancement; revalidate completion after subsequent writes or launches. Test missing IDs before valid records, an oversized destination, and upgrade → old-writer mutation → upgrade.

7. **MAJOR — §6–7, test plan §4: Large sequence writes remain unsupported when their size comes from structure.**  
   Evidence: [worker-host.ts:126](/D:/projects/ptah-extension/libs/backend/platform-electron/src/implementations/electron-state-storage-worker-host.ts:126) extracts only individual strings over 32 KiB; [worker-host.ts:403](/D:/projects/ptah-extension/libs/backend/platform-electron/src/implementations/electron-state-storage-worker-host.ts:403) sends the remaining item whole. Many smaller strings can leave that item above 262,144 estimator bytes. The existing send assertion rejects it, preventing output persistence; changing stdout handling does not repair this path.  
   **Fix:** page sequence-item structure using the scalar-operation transport, with actual envelope estimation. Add a large nested item containing only sub-threshold strings.

8. **MAJOR — §1, §7: Worker-estimator shrinking does not guarantee the RPC budget.**  
   Evidence: [worker-protocol.ts:575](/D:/projects/ptah-extension/libs/backend/platform-electron/src/implementations/electron-state-storage-worker-protocol.ts:575) charges two bytes per UTF-16 unit, whereas [session-metadata-store.ts:263](/D:/projects/ptah-extension/libs/backend/agent-sdk/src/lib/session-metadata-store.ts:263) measures JSON-encoded UTF-8. Control characters can cost six JSON bytes each. A single item shrunk to `itemBudget` by the worker estimator can still hit the unchanged single-item rejection at `:958`; that check also stringifies the oversized projection on main.  
   **Fix:** constrain projections by both estimators before returning them, reserving both envelopes and truncation metadata. Test control characters, multibyte text, and truncation suffix overhead.

9. **MAJOR — Integration architecture / rollback: The claimed `materialize-v1` implementation does not exist.**  
   Evidence: the complete request union in [worker-protocol.ts:286](/D:/projects/ptah-extension/libs/backend/platform-electron/src/implementations/electron-state-storage-worker-protocol.ts:286) and dispatch in [worker-runtime.ts:242](/D:/projects/ptah-extension/libs/backend/platform-electron/src/implementations/electron-state-storage-worker-runtime.ts:242) contain no materialization operation; repository searches found neither `materialize-v1` nor `materializeV1` implementation. “Unaffected” therefore supplies no verified rollback guarantee.  
   **Fix:** explicitly implement and round-trip-test materialization, or identify it as an unmet prior requirement and remove the unsupported compatibility claim.
