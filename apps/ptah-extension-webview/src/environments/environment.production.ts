/**
 * Production Environment Configuration
 * Used when running inside VS Code webview
 */
export const environment = {
  production: true,
  useMockApi: false,
  mockDelay: 0,
  enableLogging: false,
  apiVersion: '1.0.0',
};
