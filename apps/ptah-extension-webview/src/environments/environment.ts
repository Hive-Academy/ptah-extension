/**
 * Development Environment Configuration
 * Used when running `ng serve` or `npm run dev:webview`
 */
export const environment = {
  production: false,
  useMockApi: true,
  mockDelay: 150, // Simulate network delay (ms)
  enableLogging: true,
  apiVersion: '1.0.0',
};
