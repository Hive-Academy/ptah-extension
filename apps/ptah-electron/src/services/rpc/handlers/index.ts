/**
 * Electron RPC Handlers Index
 *
 * Exports the RPC handler classes that still live in this app.
 * Every other handler is imported directly from @ptah-extension/rpc-handlers
 * by the orchestrator and DI container.
 */

export { EditorRpcHandlers } from './editor-rpc.handlers';
